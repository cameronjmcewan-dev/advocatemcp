{
  "name": "advocatemcp-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build:knowledge": "node scripts/build-knowledge.mjs",
    "predeploy": "node scripts/build-knowledge.mjs",
    "deploy": "wrangler deploy",
    "kv:add": "wrangler kv:key put --binding=BUSINESS_MAP",
    "typecheck": "tsc --noEmit",
    "check:design": "node scripts/check-design.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.4",
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.50.0"
  }
}
