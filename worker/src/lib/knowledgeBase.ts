/* eslint-disable */
/**
 * Auto-generated by worker/scripts/build-knowledge.mjs — DO NOT EDIT BY HAND.
 *
 * Run `npm run build:knowledge` (or just `npm run deploy`, which runs
 * predeploy) to regenerate after editing site copy, docs, or DNS guides.
 *
 * Generated: 2026-04-27T04:54:23.783Z
 * Bytes:     104077
 * ~Tokens:   26019 (estimate at 4 chars/token)
 */
export const KNOWLEDGE_BASE: string = "# AdvocateMCP knowledge base\n\nThis is the canonical reference for the support assistant. Quote specifics from these sections when answering customer questions. If a customer asks about something not covered here, say so honestly and hand off to Max via email/phone/Calendly.\n\n# Marketing site copy\n\n\n## Source: site/Features.html\n\nFeatures\nEverything your business needs to show up in AI answers and nothing you have to learn.\n\nAdvocate turns the confusing world of AI crawlers into one calm dashboard. Here's what's inside.\n\nAI answers\nAlways-right answers. Straight from you.\n\nWhen ChatGPT, Claude, Gemini, or Perplexity need info about your business, Advocate serves them your real hours, prices, services, and specialties not whatever the internet guessed.\n\n- Every major AI tool pulls from one accurate source\n\n- Updates instantly when you change anything\n\n- No more outdated hours, wrong prices, or confused AI\n\nHow ChatGPT answered today\n\nChatGPT \"Is Bloom & Stem open Sunday?\"\n\nYes Bloom & Stem is open Sunday 10am–4pm. They offer same-day delivery across Austin and take walk-ins until 3pm.\n\nClaude \"Austin florist for a funeral arrangement\"\n\nFor funeral arrangements in Austin, Bloom & Stem offers same-day sympathy pieces starting at $85 and delivers directly to the service.\n\nDashboard\nOne dashboard. Real numbers. No confusion.\n\nEvery number on your dashboard comes with a plain-English explanation. A built-in tutorial walks you through it the first time you log in and you can replay it anytime.\n\n- Headline numbers: mentions, clicks, and money earned\n\n- Hover any number to see what it means\n\n- Built-in tutorial, replayable on demand\n\n- Weekly email digest so you don't have to log in to know how things are going\n\nThis week at a glance\n\nMentions\n\n412\n\n↑ 18%\n\nClicks\n\n189\n\n↑ 24%\n\nEarned\n\n$1.2k\n\n↑ 31%\n\nMentions / day Last 12 days\n\nClick-back links\nEvery AI answer ends with a link back to you.\n\nAdvocate tells AI to include a click-through link in its answer so when someone asks \"who should I hire?\", they can tap straight to your booking page, your phone number, or your contact form.\n\n- Turn AI mentions into website visitors\n\n- Track exactly which AI tool sent which visitor\n\n- Route different queries to different pages (booking, quote, menu, etc.)\n\nRecent click-throughs\n\n14:02 Perplexity · \"austin florist same day\" → order page\n\n13:48 ChatGPT · \"flowers for mom's birthday\" → homepage\n\n13:21 Claude · \"florist with online ordering\" → order page\n\n12:58 Gemini · \"open sunday flowers austin\" → hours page\n\nCompetitor Radar\nSee if AI is picking you or the shop down the street.\n\nEvery week, we check whether AI tools recommend you or your competitors for the searches that matter most. You'll see a simple win-rate chart and tips on how to pull ahead.\n\n- Track up to 10 competitors (Pro plan)\n\n- Weekly head-to-head win rate\n\n- Plain-English suggestions to improve your standing\n\n\"Best florist in Austin\" · this week\n\nYou\n\n64%\n\nComp. A\n\n22%\n\nComp. B\n\n14%\n\nTip: Add \"same-day delivery\" to your service list 3 of 5 competing answers mentioned it.\n\nRevenue attribution\nKnow which AI conversations turned into customers.\n\nTag your booking page, contact form, or checkout once and we'll show you exactly how much money came from AI recommendations. No guesswork, no spreadsheets.\n\n- Works with any website, any payment tool\n\n- See AI-driven bookings, calls, and orders\n\n- Monthly revenue report delivered to your inbox\n\nRevenue from AI · last 30 days\n\n$5,841\n\nfrom 47 bookings, up 28% vs. last month\n\nPerplexity $2,418\n\nChatGPT $1,756\n\nClaude $1,102\n\nGemini $565\n\nBuilt-in tutorial\nNobody gets left behind. Not even first-timers.\n\nThe first time you open your dashboard, a gentle walkthrough explains every section in plain English. Forget what something means? Click the help button and the tour replays exactly where you need it.\n\n- Spotlight walkthrough on first login\n\n- Replay from the help menu or the floating \"?\" button\n\n- Every number has a \"what does this mean?\" tooltip\n\n- Video tutorials and live onboarding call on Pro\n\nStep 2 of 6\n\nThese three tiles are your north-star numbers . \"Mentions\" is how many times AI brought you up this week. \"Clicks\" is how many people then visited you. \"Earned\" is the money that came from those visits.\n\nBack\nNext →\n\n💡 Tip: Press ? anytime to reopen the tutorial.\n\nTry all of it free for 14 days.\n\nNo credit card. No installation gymnastics. Just results.\n\nStart free trial\nTake the dashboard for a spin →\n\n## Source: site/Pricing.html\n\nPricing\nSimple plans. Real results.\n\nStart free for 14 days. Pick a plan when you're ready. Change or cancel anytime no phone calls, no hard feelings.\n\nPay monthly\nPay yearly save 20%\n\nBase\n\nFor single-location shops getting started with AI.\n\n$149 / month\n\nBilled monthly · cancel anytime\n\n- Accurate answers across every major AI tool\n\n- Plain-English dashboard with built-in tutorial\n\n- Click-back links & click tracking\n\n- Weekly email digest\n\n- Unlimited AI mentions\n\n- Email support (reply within 1 business day)\n\n- Competitor Radar\n\n- Revenue attribution\n\n- Multi-location\n\nStart free trial\n\nPro\n\nFor busy shops ready to out-rank the competition.\n\n$349 / month\n\nBilled monthly · cancel anytime\n\n- Everything in Base\n\n- Competitor Radar weekly head-to-head tracking\n\n- Revenue attribution see dollars from AI\n\n- Up to 3 locations\n\n- Custom click-through routing\n\n- Priority support (reply within 1 hour)\n\n- 30-minute onboarding call with a real person\n\n- Monthly performance review email\n\n- Dedicated success manager\n\nStart free trial\n\nEnterprise\n\nFor franchises, chains, and agencies.\n\nLet's talk\n\nCustom pricing based on locations\n\n- Everything in Pro\n\n- Unlimited locations\n\n- Dedicated success manager\n\n- White-glove setup for every location\n\n- Team accounts & role-based access\n\n- Custom integrations (POS, CRM, etc.)\n\n- Quarterly strategy reviews\n\n- Priority phone support\n\n- Volume pricing\n\nBook a call\n\n♥\n\n30-day happiness guarantee.\n\nIf Advocate doesn't earn you more than it costs in the first 30 days, we'll refund you. No arguments, no fine print.\n\nHow it works →\n\nCompare plans\nWhat you get, side by side.\n\nFeatures\n\nBase $149 / mo\n\nPro $349 / mo\n\nEnterprise custom\n\nCore\n\nAI-tool coverage (ChatGPT, Claude, Gemini, Perplexity)\n\nAll\n\nAll\n\nAll\n\nAccurate answers from your profile\n\n✓\n\n✓\n\n✓\n\nClick-back links in AI answers\n\n✓\n\n✓\n\n✓\n\nMonthly AI mentions\n\nUnlimited\n\nUnlimited\n\nUnlimited\n\nDashboard & insights\n\nPlain-English dashboard\n\n✓\n\n✓\n\n✓\n\nBuilt-in tutorial & help center\n\n✓\n\n✓\n\n✓\n\nCompetitor Radar\n\nUp to 10\n\nUnlimited\n\nRevenue attribution\n\n✓\n\n✓ + custom\n\nCustom click routing\n\n✓\n\n✓\n\nScale\n\nLocations\n\n1\n\nUp to 3\n\nUnlimited\n\nTeam members\n\n1\n\nUp to 5\n\nUnlimited\n\nCustom integrations (POS, CRM)\n\n✓\n\nSupport\n\nResponse time\n\n1 business day\n\n1 hour\n\nSame-day, phone\n\nOnboarding call\n\n30 min\n\nWhite-glove\n\nDedicated success manager\n\n✓\n\nStart free\n\nStart free\n\nBook a call\n\nQuestions, answered\nThings small business owners usually ask us.\n\nDo I have to know anything about AI to use this?\nNot a thing. You tell us about your business in plain English hours, services, what makes you special and we handle everything on the AI side. Your dashboard is designed for business owners, not engineers.\n\nWill I have to change my website?\nNo. Advocate runs alongside your site without touching it. If you use WordPress, Squarespace, Wix, Shopify, or anything else, it works. We install a small connector (10 minutes, zero code) and you're done.\n\nCan I really cancel anytime?\nYes. One click in your dashboard. No retention calls, no cancellation fees, no \"contact support to cancel.\" If you're within your first 30 days and Advocate didn't pay for itself, we'll refund what you paid.\n\nWhat if my industry isn't listed?\nAdvocate works for just about any local small business. If you don't see your category, book a quick call and we'll confirm usually within five minutes that we can set you up the same day.\n\nHow fast will I see results?\nYou'll see your first AI mentions within 24 hours of setup. Most customers see a clear bump in website visits within the first week and attributed revenue by week two.\n\nWhat counts as a \"location\"?\nA physical address. If you have one shop, that's 1 location. If you have a flower shop in Austin and one in Round Rock, that's 2. Service-area businesses (like plumbers who drive to customers) still count as 1.\n\nCan I talk to a real person before signing up?\nAbsolutely. Book a 15-minute call with our team and we'll walk you through the dashboard, answer your questions, and tell you honestly whether Advocate is a fit for your business.\n\nStill on the fence?\n\nBook a 15-minute call. We'll show you what AI is saying about your business right now, and what it could be saying.\n\nStart 14-day free trial\nBook a call →\n\n## Source: site/FAQs.html\n\nHelp\n\nFrequently asked questions\n\nPlain-English answers to the questions we hear most. Still stuck? Contact us →\n\nThe basics\n\nWhat does Advocate actually do?\nWe watch AI tools ChatGPT, Claude, Perplexity, Gemini, and Copilot and tell you whenever they mention your business. Then we track what happens after: did someone click through to your site? Did they book? It's like Google Analytics, but for the AI-first internet.\n\nHow do you know what AI is saying?\nWe run the questions your customers are likely to ask across every major AI tool, on a schedule. When your business shows up in an answer, we log it. We also track bot traffic to your website, so we know when an AI tool is doing research on you.\n\nIs any of this creepy or against ToS?\nNo. We use the same public AI interfaces anyone can use we're just doing it systematically and keeping records. We never pretend to be real users or automate on your behalf. Our logs come from your site's own analytics (bot traffic) and from our own AI queries.\n\nUnderstanding your numbers\n\nWhat's the difference between \"mentions\" and \"click-throughs\"?\nMentions = how many times an AI named your business in an answer. Click-throughs = how many of those mentions actually drove someone to your website. Both matter, but click-throughs are closer to dollars.\n\nHow is \"Earned\" calculated?\nWe tag every click that comes from an AI answer. If that visitor then books, orders, or calls you within 30 days, we attribute that revenue to AI. You set up the attribution once during onboarding, and then it runs automatically.\n\nWhat's the \"Win rate\" on Competitor Radar?\nFor each tracked query (like \"best florist in Austin\"), we check which businesses AI names. Win rate = how often you're in that list vs. how often a competitor is. A win rate of 64% means AI named you in 64 out of 100 answers to your tracked queries.\n\nMaking Advocate work harder for you\n\nWhat should I do if my win rate drops?\nStart with the \"Tip to pull further ahead\" on the Competitor Radar page we surface the most common keyword or service you're missing. Usually it's something simple like adding \"wedding florist\" or \"same-day delivery\" to your Business profile.\n\nHow often is my data updated?\nMentions and bot traffic are near real-time (within 2–5 minutes). The Competitor Radar runs once a day at 6am CT. Revenue attribution updates every 15 minutes once a booking completes.\n\nCan I track more than one business or location?\nYes. Click the business name in the top-left of the sidebar to switch between locations. Each location is billed separately the Base plan covers one; Pro covers up to three.\n\nBilling & account\n\nWhat's the difference between Base and Pro?\nBase ($149/mo) covers up to 20 tracked queries, 5 competitors, and 3 team seats. Pro ($349/mo) unlocks unlimited queries, 25 competitors, unlimited team, and the full Competitor Radar suite. Most multi-location businesses need Pro.\n\nCan I cancel anytime?\nYes no contracts, no cancellation fees. Cancel from the Billing page and you keep access until the end of your current billing cycle. We'll keep your data on file for 90 days in case you come back.\n\nDo you offer refunds?\nIf you cancel within the first 14 days of a new plan, we'll refund that month in full, no questions asked. After that, we don't pro-rate partial months.\n\nStill have questions?\n\nI tried everything and I'm still stuck.\nHead to the Contact page email, phone, screen-share, or live chat, whichever works for you. A real human will be in touch within 4 hours on weekdays.\n\nCan't find your answer? Contact us →\n© 2026 Advocate\n\n## Source: site/Industries.html\n\nIndustries\nWhatever you do, people are asking AI about it. Make sure they find you.\n\nAdvocate is built for the small, local businesses that make a neighborhood tick. Here's how it shows up for some of them.\n\nRestaurants\nLawyers\nDentists\nPlumbers\nElectricians\nReal estate\nFlower shops\n\nRestaurants\nBe the place AI tells hungry people to go.\n\nDiners don't search they ask . \"Where's the best brunch that takes walk-ins?\" \"Quiet dinner spot with a patio?\" When AI gets those questions, Advocate makes sure it has your menu, your hours, and your specials.\n\n2.8×\n\nMore AI mentions for dinner searches\n\n+37\n\nWeekend walk-ins from AI referrals\n\n12m\n\nTo set up no POS changes\n\nReal queries AI asks about restaurants\n\n\"Best brunch in south Austin with a patio and no wait\"\n\nChatGPT · 43 this week\n\n\"Where can I get vegan tacos tonight?\"\n\nPerplexity · 28 this week\n\n\"Restaurants open late for a birthday dinner\"\n\nClaude · 19 this week\n\n\"Family-friendly spot with high chairs\"\n\nGemini · 14 this week\n\nLawyers\nBe the first call not the tenth search result.\n\nPeople asking AI for legal help are in a hurry and anxious. Advocate makes sure your practice areas, consultation fees, and the first question they should ask are front and center in the answer.\n\n4.1×\n\nMore consultation inquiries\n\n$890\n\nAverage case value from AI leads\n\n0\n\nLines of code touched\n\nReal queries AI asks about lawyers\n\n\"Free consultation personal injury lawyer near me\"\n\nChatGPT · 52 this week\n\n\"Do I need a lawyer for a small estate in Texas?\"\n\nPerplexity · 31 this week\n\n\"Family lawyer who handles uncontested divorce\"\n\nClaude · 24 this week\n\n\"Small business attorney that works on flat fee\"\n\nGemini · 18 this week\n\nDentists\nFill your chair. This week.\n\nInsurance, hours, and \"do you take new patients?\" are the three questions AI gets all day. Advocate answers them correctly the first time, and routes curious patients straight to your online booking page.\n\n+21\n\nNew-patient bookings / month\n\n63%\n\nOf AI answers listed your practice\n\n10m\n\nSetup time, front-desk approved\n\nReal queries AI asks about dentists\n\n\"Dentist who takes Delta Dental PPO near downtown\"\n\nChatGPT · 48 this week\n\n\"Emergency dentist open Saturday\"\n\nPerplexity · 35 this week\n\n\"Family dentist accepting new patients\"\n\nClaude · 22 this week\n\n\"How much does a cleaning cost without insurance?\"\n\nGemini · 17 this week\n\nPlumbers\nOwn \"emergency plumber near me.\"\n\nWhen a pipe bursts, nobody scrolls they ask. Advocate makes sure AI has your service area, your after-hours rate, and a one-tap phone number for the panic calls.\n\n3.4×\n\nMore emergency calls from AI\n\n$420\n\nAverage ticket from AI referrals\n\n24/7\n\nAvailability reflected in answers\n\nReal queries AI asks about plumbers\n\n\"24-hour plumber for a burst pipe south Austin\"\n\nChatGPT · 61 this week\n\n\"Water heater replacement cost\"\n\nPerplexity · 29 this week\n\n\"Plumber who does slab leak detection\"\n\nClaude · 18 this week\n\n\"Licensed plumber with a flat rate\"\n\nGemini · 12 this week\n\nElectricians\nWin the big remodel jobs.\n\nHomeowners asking AI for electrical work want two things: a licensed pro and a straight answer on price. Advocate delivers both and your booking link.\n\n2.7×\n\nMore AI-sourced quote requests\n\n$2.1k\n\nAverage job value from AI\n\nDone\n\nSetup in one afternoon\n\nReal queries AI asks about electricians\n\n\"Cost to install a whole-house generator\"\n\nChatGPT · 34 this week\n\n\"Electrician for EV charger install\"\n\nPerplexity · 27 this week\n\n\"Licensed electrician for a panel upgrade\"\n\nClaude · 19 this week\n\n\"Smoke detectors keep going off who to call?\"\n\nGemini · 9 this week\n\nReal estate\nBe the agent AI actually picks.\n\n\"First-time buyer agent,\" \"agent who knows the school districts,\" \"agent who closed deals last month\" AI is asked all of it. Advocate makes sure your sales, specialties, and testimonials show up in the answer.\n\n+14\n\nBuyer inquiries per month\n\n#1\n\nIn 3 of 5 local AI answers\n\nZero\n\nTime spent on \"SEO tricks\"\n\nReal queries AI asks about real estate\n\n\"Best first-time buyer agent south Austin\"\n\nChatGPT · 26 this week\n\n\"Agent who knows Eanes school district\"\n\nPerplexity · 19 this week\n\n\"Listing agent with staging services\"\n\nClaude · 14 this week\n\n\"Investor-friendly agent duplex market\"\n\nGemini · 9 this week\n\nFlower shops\nCatch the same-day orders.\n\nBirthdays, anniversaries, sympathies AI is asked \"can I get flowers delivered today?\" every day. Advocate makes sure the answer includes your name, your cutoff time, and a link to order.\n\n+38\n\nSame-day orders / month\n\n$68\n\nAverage order from AI referrals\n\n<10m\n\nTo set up, zero tech skills\n\nReal queries AI asks about flower shops\n\n\"Same-day delivery flowers Austin under $75\"\n\nChatGPT · 41 this week\n\n\"Funeral arrangement florist with delivery\"\n\nPerplexity · 22 this week\n\n\"Florist open Sunday afternoon\"\n\nClaude · 17 this week\n\n\"Wedding florist that does small ceremonies\"\n\nGemini · 11 this week\n\nMore categories\nAdvocate works for just about any local small business.\n\nIf your customers are searching with AI, we've probably already seen queries for your industry. Don't see yours? Tell us about it we'll set you up.\n\n♨\n\nRestaurants & cafés\n\nMenus, hours, and reservations in one place.\n\n⚖\n\nLawyers\n\nPractice areas, consults, flat fees.\n\n◐\n\nDentists\n\nInsurance, hours, new-patient bookings.\n\n⚒\n\nPlumbers\n\nEmergency calls, service area, rates.\n\n⚡\n\nElectricians\n\nPanel work, EV chargers, generators.\n\n⌂\n\nReal estate\n\nSpecialties, testimonials, areas served.\n\n✿\n\nFlower shops\n\nSame-day delivery, events, occasions.\n\n✂\n\nHair salons\n\nWalk-ins, colorists, prices.\n\n⛭\n\nAuto shops\n\nServices, pricing, appointment windows.\n\n♡\n\nMedical & clinics\n\nSpecialties, accepted insurance.\n\n☂\n\nInsurance agents\n\nCoverage types, quotes, territories.\n\n✦\n\nMed spas & beauty\n\nServices, packages, intro offers.\n\n♛\n\nWedding vendors\n\nPackages, availability, calendars.\n\n☘\n\nLandscapers\n\nService areas, seasons, job sizes.\n\n◎\n\nVeterinarians\n\nSpecies, urgent care, wellness plans.\n\n+\n\nAnd 30+ more\n\nIf you serve customers, we can help.\n\nNot sure if Advocate is right for your kind of shop?\n\nBook a 15-minute call and we'll tell you honestly, with real numbers from businesses just like yours.\n\nStart free trial\n\n## Source: site/methodology.html\n\nHow we measure\n\nCitation-readiness, measured.\n\nThe number you see on your dashboard (\"Citation rating: 8.5/10\") isn't a count of real\nChatGPT citations, we don't have access to OpenAI's logs and neither does anyone else\nmaking that claim. It's a predicted score, calibrated against the rubrics AI\nengines use internally. Here's exactly how we get to it.\n\nThe format-judge harness\n\nFor every business on Advocate, we generate the same HTML + JSON-LD that real AI bots\nreceive when they crawl the site. The renderer dispatches per-engine variants, Perplexity\ngets a citation-friendly format, ChatGPT gets snippet-optimized output, Claude gets\nentity-rich blocks, Google AI Overview gets schema-heavy structured data. Same business\nfacts, four different presentations.\n\nThen we run a battery of test queries against each variant. The queries are the kinds\nof questions a real customer would ask: \"best plumber in my area\", \"tell me about\n[business name]\", \"compare X to Y\". For each query × variant combination, we ask Claude\nSonnet (acting as judge) to score the rendered output 1–10 on whether it's citation-worthy\nand why. The judge produces both a number and a written rationale.\n\nWe've iterated this harness 12+ times. Every time the judge flagged a deduction\n(\"missing third-party verification\", \"marketing language without specifics\", \"no clear\nservice inventory\"), we made a corresponding change to the renderer. The current\nscore-vs-baseline numbers we publish reflect that iteration history.\n\nWhat the score is\n\nNumber What it actually measures\n\nCitation rating\nAverage score from the Claude judge across all per-engine variants. Higher = the rendered output AI engines see has the structured signals their citation systems look for.\n\nCite rate\nThe percentage of test queries where the judge said \"yes, would cite this business\" rather than \"would skip it for a more authoritative source\".\n\nPer-engine breakdown\nSame harness run against the per-engine renderer variant. \"Perplexity 8.5/10\" means the rendered-for-Perplexity HTML scores 8.5/10, not that Perplexity actually cited the business.\n\nImportant caveat.\nThese numbers are calibrated proxies, not real citations. We can't see inside ChatGPT\nor Perplexity's retrieval pipelines and neither can any tool that promises to. What\nwe can do is measure whether the data we serve has the structured signals\ntheir citation systems are documented to use, and iterate the renderer against an\nLLM judge that approximates their scoring.\n\nThe exact judge prompt + rubric\n\nBelow is the verbatim system prompt we send to Claude Sonnet for every score on\nevery dashboard. You can copy it and run it against your own site to reproduce\nthe score yourself, or fork it and substitute a different judge model. We\npublish this so the score isn't a black-box claim.\n\nYou are evaluating a webpage's quality as a citation source for an AI search engine.\n\nYou are NOT a content marketer. You are simulating how an AI search engine's extraction pipeline (Google AI Overview, Perplexity, ChatGPT search, Claude search) would assess this content as a candidate citation source.\n\nFor each evaluation, output exactly this JSON shape and nothing else:\n\n{\n\"citability_score\": <integer 1-10>,\n\"would_cite\": <boolean>,\n\"reasoning\": \"<2-3 sentences explaining your score>\"\n}\n\nScoring rubric:\n- 10: ideal. Schema.org JSON-LD present and complete. Clear lead sentence. Self-contained citable claims. Trust signals. Action-oriented CTA. Would definitely cite.\n- 7-9: strong. Most extraction signals present, minor gaps.\n- 4-6: workable but weak. Either the prose is good but structure is missing, or vice versa. Citation likely only if alternatives are worse.\n- 1-3: poor. Hard to extract entity/facts cleanly, or mostly marketing fluff.\n\nPenalize:\n- Over-confident claims without attribution\n- Marketing hype words (\"amazing\", \"best in class\", \"world-class\")\n- AI-disclaimer hedges (\"I'm an AI\", \"based on available info\")\n- Missing structured data when the format type implies it (e.g. an HTML page with no JSON-LD)\n- JSON envelopes wrapping markdown when HTML would be more parseable\n- Bullet pages where the bullets aren't self-contained (rely on pronouns to a missing antecedent)\n\nReward:\n- Clean schema.org JSON-LD (LocalBusiness, ProfessionalService, FAQPage, Speakable)\n- Bold inline key facts (Perplexity-style)\n- Self-reported attribution preserved (\"reports\", \"states\", \"describes as\")\n- Action-specific CTA naming the verb (Book / Quote / Call / Visit)\n- First sentence &le;160 chars containing entity + specialty + location\n\nBe a tough but fair judge. Most pages should land 4-7. Reserve 9-10 for genuinely strong extraction targets.\n\nSource location in the codebase:\nserver/src/experiments/formatJudge/judges.ts (the\nSYSTEM_PROMPT constant). When the prompt evolves we increment the\nharness iteration_count on the comparison data and refresh the\npublished scores quarterly.\n\nReproducing the score against your own site\n\nIf you want to verify the methodology before signing up:\n\n- Take the system prompt above. Paste it into Claude Sonnet (or any frontier model) as a system message.\n\n- For your own site, fetch your homepage HTML and any JSON-LD it emits. Pick a likely user query like \"best [your category] in [your city]?\".\n\n- Send a user message with the format shown in judges.ts : Query: \"...\" followed by Candidate citation source: and the rendered HTML.\n\n- The model returns the same JSON shape we use. Compare the score to the scores on our comparison page.\n\nWe don't ask you to take our word for it. The harness is reproducible by any\nsufficiently determined buyer in about ten minutes. If your site scores 8.5/10\nalready with no Advocate, you don't need us. If it doesn't, the gap is what we\nclose.\n\nWhere the real-citation data is\n\nFor ground truth, actual times an AI engine cited your business in a real query,\nAdvocate runs Competitor Radar : a weekly cron that polls Perplexity's\nlive search API with curated queries for your category, then logs whether your domain\nappeared in the citations. Wins and losses are tracked over time and shown on the\nCompetitor Radar page in your dashboard. That's a different signal than the citation\nrating, and they're complementary:\n\n- Citation rating updates immediately when you change your profile,\nso you can iterate fast and see the predicted impact before any real-world signal\ncatches up.\n\n- Competitor Radar updates weekly with real Perplexity citations.\nLags by 1-2 weeks but reflects what AI engines actually do.\n\nWhat we don't claim\n\nWe don't claim that 58% of customers ask AI for [your category]. We don't claim\n\"3× more leads\" because we have no way to control for the dozens of other variables\nin your marketing mix. We don't claim that ChatGPT will definitely cite you because\nChatGPT doesn't publish its retrieval logic and would change it tomorrow if it wanted\nto. The whole point of publishing this methodology is so you can decide whether the\nproxy we measure is one you trust.\n\nWant to see it\n\nThe harness is real code.\nEvery score on every dashboard is generated by\nserver/src/experiments/formatJudge/runner.ts in our codebase, which we'll\nshare with engineering teams who want to verify the methodology before signing up.\nEmail max@advocate-mcp.com for a walkthrough.\n\n← Back to home\nSee pricing\n\n## Source: site/mcp.html\n\nMCP server\n\nAdvocateMCP\n\nA Model Context Protocol server that lets any AI assistant, Claude Desktop, Cursor, Perplexity, ChatGPT with MCP enabled, search a directory of AI-ready local businesses and query each one&rsquo;s canonical agent in real time. Neutral, platform-agnostic, zero setup per AI.\n\nEndpoint\n\nURL https://api.advocatemcp.com/mcp\n\nTransport Streamable HTTP (JSON-RPC 2.0 over HTTP POST, SSE for server-initiated messages)\n\nAuthentication None for discovery and search. Per-tenant Bearer API key is required to push writes (reservations, handoffs) back to a specific business.\n\nWhat it does\n\nLocal businesses (plumbers, law firms, agencies, copywriters, etc.) subscribe to Advocate and fill in a structured profile: services, pricing, hours, credentials, customer quotes, lead-routing preferences. Advocate publishes each profile as a standards-compliant AI advocate agent .\n\nWhen a user asks their AI assistant &ldquo;find me a good plumber in Boise who does 24/7 emergency,&rdquo; the assistant calls search_businesses , then query_business_agent on the top results. The agent answers in the business&rsquo;s own voice, with verified metadata and a trackable referral link. The AI assistant sees citation-ready answers instead of scraping a dozen inconsistent review sites.\n\nFor richer interactions, the A2A-style transactional tools ( get_availability , get_quote , reserve_slot , initiate_handoff ) let the AI assistant take action on behalf of the user, book a 30-minute window, get a price range, hand the conversation off to a human.\n\nTools\n\nTool Description\n\nsearch_businesses Search for registered businesses by category, name, or location. Returns slugs + agent endpoints.\n\nquery_business_agent Query a specific business&rsquo;s AI advocate agent. Returns a citation-ready answer with referral link.\n\nget_availability 30-minute slot windows derived from the business&rsquo;s hours_json . v1 synthetic, real-calendar integration on the roadmap.\n\nget_quote Service price quote from pricing_json_v2 . Returns an exact, range, or estimate label.\n\nreserve_slot Create a 15-minute HELD reservation; returns a signed confirmation_token the agent posts back to /a2a/confirm .\n\ninitiate_handoff Start a handoff to a human (SMS/email via tenant routing) or another agent (signed continuation URL).\n\nThe full machine-readable capability manifest, including input schemas, rate-limit declarations, and the list of business slugs, is published at /.well-known/mcp.json .\n\nQuick start\n\nClaude Desktop\n\nAdd this block to your claude_desktop_config.json :\n\n{\n\"mcpServers\": {\n\"advocate\": {\n\"url\": \"https://api.advocatemcp.com/mcp\"\n}\n}\n}\n\nCursor\n\nSettings &rarr; MCP Servers &rarr; Add server , then paste the URL above.\n\nAny MCP client\n\nPoint at https://api.advocatemcp.com/mcp . The server advertises its tools via the standard initialize &rarr; tools/list handshake; no registration is required for discovery or search.\n\nRate limits\n\n- Discovery tools ( search_businesses , query_business_agent ): 60 requests per minute per IP, burst of 100.\n\n- Transactional tools ( reserve_slot , initiate_handoff , etc.): 10 per minute per IP plus per-tenant caps.\n\n- Rate limiting is enforced at the edge by a Cloudflare Durable Object; exceeding it returns 429 with a Retry-After header.\n\nAttribution\n\nEvery referral URL returned by query_business_agent is a signed tracking token of the form https://api.advocatemcp.com/r/<token> . Clicks are recorded with the originating AI assistant (via x-agent-identity ) and the tenant can see per-AI conversion data in their dashboard. No cross-site cookies; IP addresses are salted-hashed and discarded within 30 days.\n\nFor businesses\n\nIf you run a local business and want to be findable through this server, run a free citation audit to see where AI assistants currently land when asked about your category, or head to advocatemcp.com for the 15-minute onboarding.\n\nStatus. This server is live and handling production traffic. It is in the process of being listed on the major MCP directories (Smithery, PulseMCP, Anthropic&rsquo;s registry). Questions or integration help: max@advocate-mcp.com .\n\n## Source: site/audit.html\n\nFree, 30 seconds\nSee what AI thinks of your site.\n\nOne report, two signals. We score how citation-ready your homepage is on the same Claude-judge rubric we publish, and check whether AI assistants cite you for your category. No signup.\n\nYour website\n\nYour category\n\nHow a prospect would describe you. Keep it short and natural.\n\nYour market (optional)\n\nSkip for global / online businesses. Helpful for local services.\n\nSee what AI thinks of my site\n\nMethodology and judge prompt published on methodology.html . Free, rate-limited per IP.\n\nScoring your homepage and asking AI about your category, ~30 seconds.\n\nWho's winning your category\n\nWhat AI answered\n\nMake AI cite you accurately\n\nAdvocate gives every AI crawler a structured, citation-ready answer about your business, so next time someone asks, they get your real pitch instead of scraping your HTML.\n\nClaim your agent &rarr;\n\nShare this report\n\nCopy link\n\nTrack this monthly\n\nWe'll re-run this audit every 30 days and email you what changed. One email, no marketing, easy unsubscribe.\n\nEmail me monthly\n\n## Source: site/research/state-of-ai-bot-traffic-q1-2026.html\n\nResearch · Q1 2026 Methodology Preview\n\nState of AI Bot Traffic — Q1 2026 Methodology Preview\n\nBy Cameron McEwan , Founder, Advocate · max@advocate-mcp.com\n\nPublished: April 17, 2026\nSample window: April 8–17, 2026\nLive customer: 1 (WCC)\nPilot installs: 2\nTotal queries: 64\nOrganic subset: 20\n\nExecutive Summary\n\nAdvocate is the first platform sitting on both the AI crawler path and the transaction path for small and medium businesses. We intercept bot requests at the edge, return a per-query response, and wrap every outbound citation in a cryptographically signed redirect so that the eventual human click is attributable back to the exact bot and exact query that produced the citation.\n\nThis Q1 release reports what the instrument sees in its validation phase and its first real customer week.\n\nTwo notes on scope up front, because they shape everything that follows:\n\n- Advocate has one paying customer to date — Example Agency, a copywriting agency. Every other domain that appears in the Q1 dataset is either an internal test slug or one of our own pre-customer pilot installs. We are explicit about which is which in the methodology.\n\n- We published this report at N=64 queries because documenting the instrument in its validation phase is the honest version of the story. Q2 is the first quantitative report with a multi-customer organic sample.\n\nThe attribution loop is proven to close end-to-end. Six click events captured in the validation phase, zero on the customer side in week one. The instrument works. The market side is Q2&rsquo;s job.\n\nWhy this report exists\n\nThere is a structural attribution gap in every AI-search stack shipping today.\n\nCitation-monitoring platforms — Scrunch , Profound , Peec , Otterly , Athena HQ — answer one question: &ldquo;am I being cited?&rdquo; That is necessary but not sufficient. None of them can tell an SMB whether a citation in a Perplexity answer produced a booked appointment or a revenue dollar.\n\nTraditional web analytics cannot fill the gap either. AI-referred traffic either arrives referrer-less (the user typed the URL out of the chat interface), or arrives with a referrer like chat.openai.com that tells you the surface but not the query, the bot, or the session. You cannot tie the visitor back to a specific answer the model quoted.\n\nOn the publisher side, Reddit&rsquo;s AI licensing deals with OpenAI and Google have closed a version of this loop at the content-producer tier — Reddit reportedly receives ~$60M/year from Google for training data access. But there is no Reddit-equivalent for an independent plumber in Austin, a pediatric orthotic brand, or a regional real-estate brokerage. That is the gap this research program exists to fill.\n\nThis Q1 release is a methodology preview. Its job is to publish the instrument and its first readings so the industry can evaluate the approach before the quantitative data arrives in Q2.\n\nMethodology\n\nData source\n\nAll data in this preview comes from Advocate&rsquo;s production systems covering April 8, 2026 — the first day a tenant received a live AI crawler request — through April 17, 2026.\n\nSample — three tenant types\n\nThe production instrument logged 64 total bot queries across three distinct tenant types. We break these out explicitly because they are not all the same thing and commingling them produces dishonest numbers:\n\nTenant type\nSlug(s)\nQueries\nWhat it is\n\nLive customer example-tenant (WCC) 20 Our first paying customer — a copywriting / marketing agency\n\nPilot validation install dmre , Pilot Business 38 Our own pre-customer test installs on real-sounding domains. Used to stand up, instrument, and validate the attribution pipeline before first customer release\n\nInternal test tenant redirect-test-apr12 , openai-reviewer-demo-business , final-verify-1776377843 6 Ephemeral tenants for build-out verification and third-party review demos\n\nThe pilot installs (DMRE, Pilot Business) were our own domains we used as live targets for validating the pipeline. They are not customer relationships. Treating them as customer data would be misleading. Treating them as useless test artifacts would also be misleading — the whole point of validation-phase traffic is to prove the instrument works before pointing it at customers .\n\nQuery classification\n\nWithin each tenant type we further classify each query as either ORGANIC (plausibly a real AI crawler hit) or BURST (same slug + bot + query within 10 minutes of another — our own probe traffic during validation sessions):\n\nTenant type\nTotal\nBURST\nORGANIC\n\nLive customer (WCC) 20 11 9\n\nPilot validation 38 27 11\n\nInternal test 6 — —\n\nTotal 64 38 20\n\nThe 20 ORGANIC queries are the defensible subset. All 6 tracked click events in Q1 came from the pilot-validation tier on DMRE — which is what you would expect for a pipeline running during its build-out phase. The instrument captured them correctly. That was the point of the validation.\n\nBot identification\n\nBots are identified by User-Agent at our edge. The match list is public in our manifest and includes:\n\nPerplexityBot , GPTBot , OAI-SearchBot , ClaudeBot , Google-Extended , Googlebot , anthropic-ai , cohere-ai , meta-externalagent .\n\nA matched bot is routed to our agent pipeline; everything else falls through to origin. Bot-detection results are used as routing signals only, never as authentication.\n\nIntent classification\n\nQueries are classified server-side into one of six buckets — best_top , emergency , affordable , specific_service , brand_direct , general — by a deterministic heuristic. This is server-side inference, not user labeling.\n\nAttribution\n\nEvery citation link returned in a bot response is wrapped in a cryptographically signed redirect token resolved at our edge. When a human clicks the link, the click is logged against the originating bot query and issued a 302 to the destination. Tokens carry no user-identifiable data and are verified without a database round-trip.\n\nCaveats — unignorable\n\n- Sample size. Live-customer ORGANIC N is 9 queries. Nothing here is statistically conclusive. Everything we report is directional and invites disproof.\n\n- One customer. Q1 has one paying customer. Vertical, regional, and cross-tenant generalizations are impossible until Q2.\n\n- Observation window. Nine days. Crawler behavior varies week-to-week; we do not yet have the baseline to normalize.\n\n- Clicks came from pilot installs. The validation-phase clicks (6 events, 3 queries, all DMRE) prove the loop closes but do not tell us anything about customer-facing conversion.\n\nFinding 1 — Live customer bot distribution (WCC, N=20)\n\nOn our first paying customer&rsquo;s domain, across 20 total queries (11 BURST + 9 ORGANIC), the bot distribution was:\n\nBot\nWCC queries\nShare\n\nPerplexityBot 12 60.0%\n\nClaudeBot 5 25.0%\n\nGPTBot (all versions) 3 15.0%\n\nTotal 20 100%\n\nWhat the live-customer data suggests (with heavy caveat): at this scale and for this type of business (B2B copywriting / marketing agency), PerplexityBot was the dominant AI crawler , not GPTBot. ClaudeBot&rsquo;s showing is non-trivial. GPTBot is a minority.\n\nThis contradicts the conventional wisdom that GPTBot dominates AI crawler volume. We are not claiming it generalizes — N is tiny and one vertical isn&rsquo;t a market — but it is the first customer-tier data point in print.\n\nContrast: pilot validation distribution\n\nOur own pre-customer pilot installs (DMRE + Pilot Business, N=38) saw a completely different mix: GPTBot 79%, PerplexityBot 16%, mcp-client 5%. That distribution is heavily shaped by how we ran build-out probes (GPTBot-simulated curl tests dominated the early validation sessions) and is not a read on real crawler behavior. Reporting it alongside the customer data would misrepresent both.\n\nThe gap between the two distributions is exactly the reason we insist on separating validation traffic from customer traffic.\n\nFinding 2 — The attribution loop closed end-to-end (in validation)\n\nSix click events were captured in click_events during the Q1 window. Three distinct queries received clicks. All three were on our pilot-validation install (DMRE), all via PerplexityBot sessions:\n\nQuery id\nBot\nTenant type\nQuery text\nRead\n\n28 PerplexityBot Pilot validation &ldquo;do you specialize in land brokerage in Austin&rdquo; Inside a burst pattern with id 27 — our probe\n\n30 PerplexityBot Pilot validation &ldquo;do you handle commercial land sales&rdquo; Organic within the validation session\n\n31 PerplexityBot Pilot validation &ldquo;final session 1 verification&rdquo; Query text is a manual-test tell — our verification click\n\nHonest headline: the validation phase proved the attribution pipeline closes end-to-end. A bot query → signed citation response → human click → attributed back to the originating bot + query — all captured. The instrument works.\n\nWhat this does not prove: that customer-facing bots will click, at what rate, which bots convert better, or anything about commercial value per crawler hit. That is Q2&rsquo;s work.\n\nWhy no WCC clicks yet?\n\nOur first paying customer was onboarded mid-window, has 9 ORGANIC queries in the dataset, and logged 0 attributed clicks. At N=9 over nine days, that is within the noise floor — a 10% click rate would produce 0–1 clicks. It is statistically uninformative. Q2 will have the volume to start separating signal from noise.\n\nFinding 3 — Intent distribution across validation + customer traffic\n\nThe intent classifier operates per-query regardless of tenant type. Across the 20 ORGANIC queries (all tenant types combined):\n\nIntent bucket\nCount\nShare\n\ngeneral 10 50.0%\n\naffordable 7 35.0%\n\nbrand_direct 2 10.0%\n\nbest_top 1 5.0%\n\nemergency 0 0.0%\n\nspecific_service 0 0.0%\n\nThe general + affordable dominance (85% combined) indicates AI crawlers at this stage of SMB exposure are in catalog-building mode — &ldquo;what does this business do&rdquo; and &ldquo;how much does it cost&rdquo; — rather than recommendation mode.\n\nOne interesting non-finding: emergency intent showed up twice in the raw 64-query dataset (plumber / HVAC-adjacent language on our pilot installs) but both instances fell into BURST clusters and disappear from the ORGANIC subset. The &ldquo;AI assistants are being used for 2am burst-pipe queries&rdquo; pattern from plumbers is a real anecdotal report we wanted to test; at Q1 volume we cannot. Q2 with home-services tenants onboarded will.\n\nWhat Q2 will measure\n\nThe Q2 report lands in July 2026. Between now and then we will onboard more customers and materially expand the defensible sample.\n\n- Cross-bot conversion deltas at customer scale — first quantitative test of whether PerplexityBot&rsquo;s apparent dominance on the WCC sample holds across verticals.\n\n- Per-agent attribution. Our per-agent reputation rollup shipped in April 2026. Q2 is the first report where we can attribute outcomes to specific caller agents (Claude Desktop vs. Cursor vs. ChatGPT Apps vs. generic MCP client) over 7-day and 30-day windows.\n\n- Competitor Radar cohort data. Our weekly Perplexity-poll loop auto-seeds a query basket per Pro tenant and records whether the tenant&rsquo;s domain showed up. Q2 will include the first cohort-level read on which citation battles SMBs systematically win vs. lose.\n\n- Vertical and regional variation. Once tenants span more verticals and states, we can report on whether crawler pressure correlates with regional AI-adoption or tracks legacy SEO patterns.\n\n- MCP-client-originated transactions. With over 10,000 public MCP servers in the registry as of April 2026 , Claude Desktop and Cursor are the leading edge of a shift where a meaningful fraction of SMB queries arrive as MCP tool calls, not as user-agent-identified crawler requests. Q2 will report on that ratio for the first time.\n\nAbout Advocate\n\nAdvocate intercepts AI crawler traffic at the edge, generates per-bot per-query optimized responses, and tracks every resulting citation click end-to-end. We also expose all registered businesses through a central MCP server at /mcp so MCP-compatible clients like Claude Desktop and Cursor can query any tenant directly. The free Business tier covers agent hosting and attribution. Pro adds Competitor Radar (weekly Perplexity citation polling) and per-agent reputation analytics.\n\n- Web: advocatemcp.com\n\n- Contact: max@advocate-mcp.com\n\nJournalists and AI-platform partnerships leads: we will share a de-identified aggregate export on request so you can evaluate any claim against the underlying data.\n\nMethodology appendix\n\nAttribution\n\nEvery outbound link in a bot response is wrapped in a cryptographically signed redirect token resolved at our edge. When a human clicks the link, the click is logged against the originating bot query and the user is 302&rsquo;d to the destination. Tokens carry no user-identifiable data and are verified without a database round-trip.\n\nBot detection\n\nUser-agent matching at the edge against a curated list of AI crawlers. The match logic is intentionally boring — no TLS fingerprinting or IP-reputation lookups — because the cost of a false negative (real bot routed to origin) is small and the cost of a false positive (human routed into the bot pipeline) is large. Bot detection is a routing signal only, never an auth primitive.\n\nClick deduplication\n\nDeduplication happens at query time, not write time. A click is unique within a 24-hour window by the tuple (token, client-IP prefix, user agent). We prefix-truncate the IP before hashing so we cannot reconstruct a full user IP from the logs.\n\nIntent classification\n\nA small deterministic rule set over the lowercased query string returning one of six buckets. Not an LLM classifier — a heuristic we can audit and update without a deploy. Both the bucket label and the raw query are stored so historical rows can be re-classified when rules change.\n\nTenant-type classification for this report\n\nLIVE / PILOT / TEST tenant-type classifications were applied post-hoc to the production queries table using the slug allowlists documented in the Methodology section. BURST detection uses a 10-minute window on matching (slug, bot, query) tuples. Neither classifier is (yet) a first-class piece of the production stack; Q2 will include them as durable views alongside raw query data.\n\nVerification\n\nJournalists, AI-platform partnerships teams, and research reviewers can request a methodology walk-through call at max@advocate-mcp.com . We can demonstrate the pipeline end-to-end against a live tenant without disclosing proprietary implementation detail.\n\nShare this report\n\nIf you know an SMB founder, marketer, or reporter working on AI search, send it their way.\n\nShare on X\n\nShare on LinkedIn\n\nSubscribe for Q2\n\n&copy; 2026 Advocate. Privacy · Terms · Contact\n\n# Public documentation\n\n\n## Source: docs/attribution.md\n\n# Attribution\n\n## Current state (post Session 1 — shipped 2026-04-10)\n\nThe attribution system is a closed end-to-end loop: bot query → signed response → human click → logged click event → query row updated. All four Session 1 gaps are closed and verified in production.\n\n## Full flow\n\n1. An AI crawler hits a customer domain. The Worker resolves the slug via KV and proxies the query to Railway's `POST /agents/:slug/query`.\n2. Railway runs the Claude agent, inserts a row into `queries`, and captures the `lastInsertRowid` as `query_id`.\n3. Railway builds a signed attribution token via `buildToken()` in `server/src/lib/tracked-url.ts`. Token payload: `{ dest, ref, slug, query_id, ts }`. Signed with HMAC-SHA256 over the base64url-encoded payload string using `TOKEN_SIGNING_KEY`.\n4. Railway returns `{ response, referral_url, query_id, attribution_token, ... }` to the Worker.\n5. The Worker UTM-tags the `referral_url` and builds the tracking URL. If `attribution_token` is present (always in normal operation): `GET /track?t=<token>`. If absent (degenerate fallback): `GET /track?to=<utm-dest>&ref=<bot>&client=<slug>`. The tracking URL is returned to the crawler as `tagged_referral_url`.\n6. The crawler includes the link in its answer to the human user.\n7. Human clicks → browser hits `GET /track` on the Worker.\n8. **Signed path** (`?t=`): Worker calls `verifyToken()` in `worker/src/lib/tracked-url.ts`. Verifies HMAC byte-for-byte, rejects if token is > 90 days old. On success: fires `ctx.waitUntil(POST /analytics/:slug/referral-click)` with `{ ref, user_agent, ip_hash, destination, query_id, legacy: 0 }`. Logs `track_signed_click` metric. Redirects to `payload.dest`.\n9. **Legacy path** (`?to=`): fires the same POST with `{ ref, user_agent, ip_hash, legacy: 1 }`. Logs `track_legacy_click` metric. The legacy path stays live until `legacy=1` rows in `click_events` decay to zero.\n10. **Verification failure** (bad HMAC / expired / malformed): logs `track_verification_failure` with reason, redirects to best-effort destination via `safeFallbackDest()` (decodes payload half if parseable, falls back to `apiBase`). No click is logged.\n11. Railway's `referral-click` handler: cross-tenant guard checks `query_id` belongs to `slug` before touching anything. Inserts into `click_events (business_slug, ref, user_agent, ip_hash, destination, query_id, legacy)`. Updates `queries.referral_clicked = 1 WHERE id = query_id` in the same statement.\n\n## Token format\n\n```\n/track?t=<token>\ntoken      = <encodedPayload>.<encodedSig>\nencodedPayload = base64url(JSON.stringify({ dest, ref, slug, query_id, ts }))\nencodedSig     = base64url(HMAC-SHA256(ASCII_bytes(encodedPayload), TOKEN_SIGNING_KEY))\n```\n\n**Critical**: HMAC is computed over the ASCII bytes of the `encodedPayload` string, not the raw JSON bytes and not the decoded bytes. Both `server/src/lib/tracked-url.ts` and `worker/src/lib/tracked-url.ts` include an explicit comment explaining this — it is the most common cross-implementation drift point. A shared test vector (`KNOWN_TOKEN`) in both test files enforces parity; if either environment produces a different result, the signing logic diverged. Do not update the constant — fix the code.\n\nToken lifetime is 90 days (`ts` checked at verification time).\n\n## Session 0 primitives — request_id and `aid` token claim\n\nTwo small primitives threaded through the attribution path so every request has a stable correlation handle and every token can (optionally) carry one. Both are additive — they do not change any existing on-wire format for callers that ignore them.\n\n### `x-advocate-request-id` header\n\nEvery request into Railway goes through the `requestId` helper at [server/src/lib/requestId.ts](server/src/lib/requestId.ts), which:\n\n- Accepts an inbound `x-advocate-request-id` header if present **and** well-formed (ULID — 26 chars, Crockford base32). Malformed values are discarded silently rather than rejected, so an upstream caller passing garbage never breaks the request.\n- Mints a fresh ULID if no valid inbound ID is present.\n- Echoes the resolved ID back on the response as the same `x-advocate-request-id` header, so clients and the Worker can pin logs to the same ID the server persisted.\n- Persists the resolved ID into the `queries.request_id` column on the `queries` insert, giving every query row an end-to-end correlation handle that spans Worker logs, Railway logs, and the DB.\n\nULID is chosen (over UUIDv4) for lexicographic time-ordering — useful for log scans and for debugging bot-burst patterns without a separate timestamp join. The helper's header read is case-insensitive per HTTP semantics; header writes use the lowercase canonical form.\n\n### Optional `aid?: string` claim on the signed token\n\nThe token `TokenPayload` in [server/src/lib/tracked-url.ts](server/src/lib/tracked-url.ts) and [worker/src/lib/tracked-url.ts](worker/src/lib/tracked-url.ts) gains an **optional** `aid?: string` field for per-assistant / per-agent attribution (e.g. distinguishing different Claude-powered surfaces that share a slug).\n\n**Back-compat guarantee — byte-identical tokens when `aid` is absent.**\n\nA token minted before Session 0 and a post-Session-0 token built from the same `{dest, ref, slug, query_id, ts}` payload with `aid` omitted must be byte-for-byte identical — same `encodedPayload`, same signature, same full token string. This matters because:\n\n- Cached / in-flight tracking URLs issued before Session 0 must continue to verify without re-signing.\n- The cross-env parity test vector (`KNOWN_TOKEN`) in both `tracked-url.test.ts` files is an existing pre-`aid` token and must not change.\n\nThe `buildToken` implementation achieves this by omitting `aid` from the JSON payload when the caller does not pass it (i.e. `aid` is `undefined`, not `null` or `\"\"`). `JSON.stringify` already drops `undefined` property values, so the serialized string is identical to the pre-Session-0 form. Do not \"normalize\" the payload by inserting `aid: null` — that would break every pre-Session-0 token on the next verification.\n\nVerification on the Worker side accepts tokens with and without `aid` — the field is only read when present, and its absence is not a rejection reason.\n\n## Structured log metrics on /track\n\n| Metric | When emitted | Key fields |\n|--------|-------------|-----------|\n| `track_signed_click` | Signed token verified, human UA, click logged | `slug`, `query_id`, `ts` |\n| `track_legacy_click` | Legacy `?to=` path, human UA, click logged | `slug` |\n| `track_verification_failure` | Bad HMAC, expired, or malformed token | `reason` (`bad_signature` \\| `expired` \\| `malformed`) |\n\nUse `track_signed_click` / (`track_signed_click` + `track_legacy_click`) ratio to monitor legacy decay. When legacy ratio ≈ 0 for 30+ days, the legacy fallback code in `worker/src/index.ts` can be removed.\n\n## Database tables involved\n\n- `queries.referral_clicked` — updated to 1 on click (via `query_id` linkage)\n- `queries.intent` — classified at query time, stored per row\n- `click_events` — one row per human click: `business_slug, ref, user_agent, ip_hash, destination, query_id, legacy`\n\n## Key files\n\n| File | Role |\n|------|------|\n| `server/src/lib/tracked-url.ts` | `buildToken()` — Node crypto, synchronous HMAC-SHA256 |\n| `server/src/lib/tracked-url.test.ts` | 6 tests; holds `KNOWN_TOKEN` reference constant |\n| `worker/src/lib/tracked-url.ts` | `verifyToken()`, `base64urlToBytes()` — Web Crypto, async |\n| `worker/src/lib/tracked-url.test.ts` | 7 tests; same `KNOWN_TOKEN` constant — cross-env parity check |\n| `server/src/routes/analytics.ts` | `referral-click` handler — cross-tenant guard, `click_events` insert, `queries` update |\n| `worker/src/index.ts` | `/track` dual-path handler, `safeFallbackDest()`, `trackingUrl` construction |\n\n## Known issues — deferred from Session 1\n\nThese bugs were discovered during Session 1 final verification when testing `customers.advocatemcp.com/agents/:slug/query` directly. They are not regressions introduced by Session 1. They do not affect the primary bot-detection flow (customer domain → KV lookup → Railway agent), which was not directly testable without DNS. Both bugs live in the Worker proxy path used when `customers.advocatemcp.com` is the integration surface.\n\n**Bug 1 — Malformed proxy URL (`/agents/agents/query` instead of `/agents/:slug/query`).** [RESOLVED 2026-04-10 — Phase 1.5]\nWhen a request hits `customers.advocatemcp.com/agents/dmre/query` with a crawler User-Agent, the Worker's first-path-segment slug fallback at `worker/src/index.ts` (previously around line 349) resolved the slug as `\"agents\"` because `\"agents\"` was not in the `RESERVED` set. That produced a downstream URL of `https://advocate-production-2887.up.railway.app/agents/agents/query` on Railway, which 404'd. Fixed in Phase 1.5 by adding a dedicated `POST /agents/:slug/query` route at `index.ts` dispatch step (2c), scoped via `WORKER_HOSTNAMES` to platform hostnames only so the bot-detection flow on real customer domains stays unchanged. The slug is now pulled from the URL path explicitly. `\"agents\"` was also added to the `RESERVED` set as belt-and-suspenders defense against a future removal of the dedicated route silently re-introducing the bug.\n\n*Note on trigger conditions discovered during Phase 1.5 verification*: the bug only manifests when the request carries a crawler User-Agent. A non-crawler UA hits the non-crawler short-circuit at `index.ts:310` and returns a 200 \"Non-crawler request — no agent response generated\" info response before ever reaching the slug fallback. This means any smoke test exercising the proxy path must send a crawler UA (e.g. `GPTBot/1.1`) or the assertion is meaningless. Documented here so future session prep doesn't re-discover this the hard way.\n\n**Bug 2 — Missing `X-API-Key` forwarding on the proxy path.** [RESOLVED — already fixed in code at the time of Phase 1.5 investigation]\nPhase 1.5 investigation on 2026-04-10 found that the `/agents/:slug/query` fetch in `worker/src/index.ts` (at the bot-detection path) **already** forwards `env.API_KEY` as `X-API-Key`:\n\n```typescript\nheaders: {\n  \"Content-Type\": \"application/json\",\n  ...(env.API_KEY ? { \"X-API-Key\": env.API_KEY } : {}),\n}\n```\n\nThe original bug description in this doc referred to \"a different code path serving the proxy\" but no such second code path exists in current `main` — there is only one `/agents/:slug/query` proxy path in the worker and it forwards the header correctly. An exhaustive grep of every `fetch()` call to Railway in `worker/src/` confirmed this (see the Phase 1.5 commit body for the full grep table). Either the bug was silently resolved during Phase 1 or Phase 2 refactoring and nobody updated this doc, or the original bug description was slightly off. Phase 1.5 added a smoke test that exercises the direct-proxy path end-to-end with both Bug 1 and Bug 2 as failure modes, so any regression in either direction will be caught.\n\n## Production state observations — not bugs, flagged for future sessions\n\n**API_KEY runtime drift (discovered and resolved 2026-04-10).**\nDuring Phase 1.5 pre-commit verification, a live test against the deployed worker revealed that Railway was rejecting the worker's `X-API-Key` with `401 Invalid or missing api_key` — not because the worker code was missing the header (it wasn't), but because the runtime value of `env.API_KEY` on the deployed worker did not match `process.env.API_KEY` on Railway. The drift manifested as every worker → Railway agent query (including the bot-detection fallback path on `customers.advocatemcp.com/dmre`) returning `502 Backend returned an error` with a nested `401` from Railway. The drift did not affect customer onboarding or portal auth — those paths use separate credentials — but it did break every production crawler query from the worker until reconciliation.\n\nThe root cause of the drift is unknown — it could have originated from a worker-side rotation that was not propagated to Railway, or a Railway-side rotation that was not propagated to the worker. We are not speculating. The drift was reconciled on 2026-04-10 during Phase 1.5 by setting the same value on both sides and verifying the worker → Railway chain end-to-end.\n\nLessons and operational guidance:\n\n1. A smoke test that hits the full worker → Railway chain end-to-end would have caught this when it first appeared. Phase 1.5 adds exactly that smoke test — see `worker/scripts/smoke-test.sh` section 9.\n2. Any production state change that involves a shared secret must update both sides (worker wrangler secret AND Railway env var) in the same operation, and must be immediately followed by a cross-check call that exercises the full chain. Unilateral updates silently create drift that is invisible until a specific code path is exercised.\n3. Static code analysis (\"the code forwards the header\") is necessary but not sufficient — runtime behavior may differ and requires a live call. This applies to any runtime-injected value, not just API keys.\n\n**Missing `X-API-Key` on `/track` → `/analytics/:slug/referral-click`.**\nThe `/track` click logger in `worker/src/index.ts` at both the signed-token path and the legacy cleartext path POSTs to `${apiBase(env)}/analytics/:slug/referral-click` **without** forwarding `X-API-Key`. This is intentional as of 2026-04-10 and should not be \"fixed\" without a dedicated review of the `/track` endpoint's auth model. The `/track` endpoint is designed to be invoked by the end-user's browser during a referral-click redirect, not by a trusted server, so it has historically been public-ish on the Railway side. If a future session decides to lock down `/analytics/:slug/referral-click` behind server auth, the worker's `/track` handler will need to be updated at the same time and every currently-issued tracking URL in customer responses will need to be regenerated. Do not touch this without an explicit session scope — the blast radius is every referral click in flight.\n\n## Session 5 — AI Handoff (client-side intent script)\n\n**Shipped 2026-04-18.** Landing pages on customer domains can read an AI-referred visitor's intent + referring crawler + slug without any server-side integration.\n\n### Flow\n\n1. Perplexity / ChatGPT / etc. cites the tenant's agent response. The response contains a `/track?t=<signed-token>` URL.\n2. A human clicks that URL. The worker's `/track` handler verifies the token, logs the click, then **redirects to `${dest}?amcp_t=<token>`** (not bare `dest`) — the token is forwarded on the redirect for human traffic only. AI-crawler redirects skip the token since crawlers don't run JS.\n3. The customer's landing page has included `<script src=\"https://advocatemcp.com/advocate-context.js\">` once. On load the script reads `?amcp_t=` from the URL, GETs `${apiBase}/r/<token>/decode`, and writes the result to `window.advocateContext`.\n4. The script also dispatches a `CustomEvent('advocate:context', { detail })` on `window` and strips the `amcp_t` param from the URL via `history.replaceState`.\n\n### `GET /r/:token/decode` (Railway)\n\n- Verifies the HMAC on `TOKEN_SIGNING_KEY` via `server/src/lib/tracked-url.ts::verifyToken`.\n- Joins `queries.intent` by the token's `query_id`. Returns `intent: null` if the row is missing or unclassified.\n- Returns only `{ intent, ref, slug }`. Never exposes `dest`, `query_id`, `aid`, or any other token claim. CORS: `Access-Control-Allow-Origin: *`. Cache-Control: `private, max-age=60`.\n- Rejections: `400 { error: \"invalid_token\", reason: \"malformed\" | \"bad_signature\" | \"expired\" }`.\n\n### Script configuration\n\nAll optional, via data attributes on the `<script>` tag:\n\n| Attribute | Default | Purpose |\n|---|---|---|\n| `data-api-base` | `https://api.advocatemcp.com` | Override for self-hosters |\n| `data-param-name` | `amcp_t` | URL query-param key the script reads |\n| `data-strip-url` | `true` | Set to `\"false\"` to leave the token in the URL after decoding |\n\n### Customer integration example\n\n```html\n<script src=\"https://advocatemcp.com/advocate-context.js\"></script>\n<script>\n  window.addEventListener(\"advocate:context\", function (e) {\n    var ctx = e.detail; // { intent, ref, slug }\n    if (ctx.intent === \"emergency\") {\n      document.querySelector(\"#hero-cta\").textContent = \"Call us right now\";\n    } else if (ctx.intent === \"affordable\") {\n      document.querySelector(\"#hero-cta\").textContent = \"See our pricing\";\n    }\n    document.body.setAttribute(\"data-ai-ref\", ctx.ref);\n  });\n</script>\n```\n\n### Privacy posture\n\n- Token carries no PII — only `{ dest, ref, slug, query_id, ts, aid? }`.\n- Decode response deliberately excludes `dest` and `query_id` so a customer-owned JS context never sees fields that would let it reconstruct the click destination or correlate across tenants.\n- Token is stripped from the URL on first decode so `document.referrer` on subsequent navigations doesn't leak it to third parties.\n- Endpoint is not rate-limited beyond the shared `rateLimitMiddleware` on Railway; revisit if abuse appears.\n\n## Updating this doc\n\nUpdate this file at the end of any session that touches `/track`, `click_events`, `tracked-url.ts`, `/r/:token/decode`, `advocate-context.js`, or the `referral-click` endpoint.\n\n## Source: docs/bot-detection.md\n\n# Bot Detection\n\n## What exists today\n\nBot detection runs in the Cloudflare Worker at `worker/src/index.ts`. The `AI_CRAWLERS` constant lists nine user-agent substrings matched case-insensitively against every incoming `User-Agent` header: `PerplexityBot`, `GPTBot`, `OAI-SearchBot`, `ClaudeBot`, `Google-Extended`, `Googlebot`, `anthropic-ai`, `cohere-ai`, `meta-externalagent`. The `isAiCrawler(ua)` function returns a boolean; `crawlerName(ua)` returns the matched string or `null`. Both are used in the main fetch handler to branch routing.\n\nDetection is purely user-agent based — no IP allowlist, no TLS fingerprinting. This is intentional: the goal is routing signal, not access control. Bot detection results are never trusted as authentication; they only determine whether a request is forwarded to the agent API or returned an info response. The Worker also uses `crawlerName` to populate the `botType` field in the structured analytics log (JSON to Cloudflare Logpush) and the `utm_content` UTM parameter on tagged referral URLs.\n\n## Request routing (checked in order)\n\n1. Portal routes (`/login`, `/auth/*`, `/dashboard`, `/api/client/*`, `/admin/create-client`) — always human/API, checked first\n2. Demo routes (`/demo`, `/demo/search`, `/demo/:slug`) — public, no crawler logic\n3. `/track` redirect — click logging + 302 to destination; bot-filtered (only logs non-crawler UAs)\n4. `/mcp` and `/mcp/*` — proxied straight to Railway, bypasses crawler detection\n5. `/.well-known/ai-agent.json` — served to all UAs including crawlers\n6. Non-crawler traffic — returns a 200 JSON info response (not a passthrough)\n7. AI crawler traffic — KV slug lookup → optional tenant status check → backend proxy\n\n## What to do when updating\n\nDo not modify `AI_CRAWLERS` or the detection functions without explicit instruction from the repo owner. Changes here affect every downstream routing decision. If a new crawler needs to be added, propose it with the exact user-agent string from that crawler's published documentation and wait for approval before touching the array.\n\n## Source: docs/dns-routing.md\n\n# DNS routing for tenant custom hostnames\n\n## The problem\n\nAI crawler traffic to tenant custom hostnames (e.g. `www.example-tenant.com`) returned 522 at Cloudflare's edge in ~67ms — too fast to be an upstream timeout. CF was refusing to route the request to our Worker, even though:\n\n- `status: active`, `ssl_status: active` on the custom hostname record\n- `fallback_origin` was set and active to `customers.advocatemcp.com` since April 9\n- `custom_origin_server` was set to `customers.advocatemcp.com`, a proxied DNS record in our SaaS zone (advocatemcp.com) bound to a Worker Route pattern `customers.advocatemcp.com/*`\n\nA prior attempt to catch this traffic with a zone-wide `*/*` Workers Route captured the marketing Pages site by mistake and was reverted in commit `bbbf572`. A second attempt (`31f84b0`) pointed `custom_origin_server` at the Worker's workers.dev URL to escape a theorized same-account-zone loopback — CF silently rejected it with `verification_errors: \"The custom origin hostname you specified does not exist on Cloudflare as a DNS record in your zone: advocatemcp.com\"`. CF SaaS requires `custom_origin_server` to be an A/AAAA/CNAME record inside the SaaS zone.\n\n## The actual fix\n\nTwo pieces are needed for a tenant hostname to route through CF SaaS to our Worker:\n\n1. **CF SaaS custom hostname record** with `custom_origin_server = \"customers.advocatemcp.com\"` and `ssl.settings.min_tls_version = \"1.2\"`. Managed declaratively via `worker/src/lib/hostnameSpec.ts` and reconciled on every `POST /admin/domains/activate` call.\n\n2. **A Worker Route `{tenant-hostname}/*` bound to our zone `advocatemcp.com`, script `advocatemcp-worker`.** Without this, CF SaaS has no Worker target to forward to: the request's URL hostname is the tenant domain (not `customers.advocatemcp.com`), so our existing `customers.advocatemcp.com/*` route pattern never matches. CF's edge returns a fast 522 without attempting any upstream.\n\nThe Worker Route does NOT need to be `*/*` (which breaks Pages). A per-tenant pattern is fine because CF SaaS custom hostnames are delegated to our zone for routing purposes — CF accepts `www.example-tenant.com/*` as a valid route pattern on zone `advocatemcp.com` even though the domain itself is external.\n\n## How it works now\n\nOn every `POST /admin/domains/activate` call:\n\n1. The CF custom hostname record is created or reconciled toward `desiredHostnameSpec()`. If drift is detected (missing `custom_origin_server`, wrong TLS floor, etc.) a targeted PATCH fires. See `worker/src/lib/reconcileHostname.ts`.\n2. *(Currently manual until CF_API_TOKEN is rescoped — see followups.md)* A Worker Route `{hostname}/*` is created pointing to `advocatemcp-worker`. Today this must be added via the CF dashboard: `advocatemcp.com` zone → Workers Routes → Add route.\n\nThe activate response returns `reconcile_summary` when drift was patched:\n\n```json\n{\n  \"ok\": true,\n  \"status\": \"already_exists\",\n  \"reconcile_summary\": {\n    \"patched\": true,\n    \"drift\": [\"custom_origin_server\"]\n  }\n}\n```\n\n## Admin endpoints\n\nAll require `X-Admin-Secret` header.\n\n- `POST /admin/domains/activate` — register/reconcile the CF SaaS hostname.\n- `GET /admin/domains/:slug/status` — synthesized verification/SSL status view.\n- `GET /admin/domains/:slug/raw` — raw CF custom hostname record plus zone `fallback_origin`. Use this when `/status`'s summary hides a field (e.g. `verification_errors`).\n- `POST /admin/domains/saas-fallback-origin` — idempotent PUT of the zone-level SaaS fallback origin. Body `{\"origin\": \"hostname\"}` (defaults to `customers.advocatemcp.com`).\n- `POST /admin/domains/ensure-worker-route` — creates the per-tenant Worker Route. Body `{\"hostname\": \"www.example.com\"}`. Currently returns `Authentication error` because `CF_API_TOKEN` lacks `Workers Routes: Edit` scope; see followups.md to re-scope.\n\n## Failure modes\n\n- `cf_api_error` (502) — initial CF POST or GET failed.\n- `cf_reconcile_error` (502) — CF PATCH during reconciliation failed. The hostname state is unchanged; safe to retry.\n\nLog events in `wrangler tail`:\n- `hostname_reconciled` — successful PATCH with the drift array.\n- `hostname_reconcile_failed` — PATCH failure with the error detail.\n\n## Adding a new CF SaaS field to the spec\n\nIf Cloudflare introduces a new required field (e.g. bot-management class, edge cert profile):\n\n1. Add it to `CustomHostnameSpec` in `worker/src/lib/hostnameSpec.ts`.\n2. Add a drift-check branch to `reconcileHostname.ts`.\n3. Add a unit test for the new drift case.\n4. Deploy. Every tenant converges on the new spec on their next activate-call touch.\n\nNo migration script needed. Reconciliation is the migration.\n\n## Backfilling a broken tenant\n\nTo fix a hostname in a broken state:\n\n1. **CF SaaS record drift** (missing `custom_origin_server`, wrong TLS, etc.): re-call the activation endpoint. The response's `reconcile_summary.drift` will list exactly which fields were fixed.\n\n   ```bash\n   curl -X POST https://customers.advocatemcp.com/admin/domains/activate \\\n     -H \"X-Admin-Secret: $ADMIN_SECRET\" \\\n     -H \"Content-Type: application/json\" \\\n     -d '{\"domain\":\"www.example.com\",\"slug\":\"example-slug\",\"origin_url\":\"https://www.example.com\"}'\n   ```\n\n2. **Missing Worker Route** (bot traffic returns 67ms 522 despite CF record being clean): add the route via CF dashboard or POST `/admin/domains/ensure-worker-route` (once CF_API_TOKEN is re-scoped).\n\n3. **Diagnostic first** when unsure: `GET /admin/domains/:slug/raw` exposes `verification_errors` and `fallback_origin` that the synthesized `/status` endpoint hides.\n\n## Source: docs/mcp-server.md\n\n# MCP Server\n\n## What exists today\n\nThe central MCP server lives at `POST /mcp` and `GET /mcp` on the Railway Express backend (`server/src/routes/mcp.ts`). It uses `@modelcontextprotocol/sdk` with the `StreamableHTTPServerTransport` (stateless mode — a new `McpServer` instance is created per request to avoid shared-transport concurrency issues). The Worker proxies all `/mcp` and `/mcp/*` paths straight through to Railway without modification, so the MCP endpoint is accessible at both the Railway URL and the Worker's public hostname.\n\n## Tools exposed\n\n**`query_business_agent`** — accepts `{ slug: string, query: string }`. Looks up the business in SQLite, calls `queryAgent()` (same code path as the crawler flow), and returns the Claude response as a JSON text block. Returns an `isError: true` result if the slug is not found.\n\n**`search_businesses`** — accepts `{ search: string, location?: string }`. Runs a LIKE search across `name`, `description`, `services`, and `category` in the SQLite `businesses` table (with optional `location` filter). Returns up to 20 matches with slug, name, description, category, location, website, star_rating, review_count, pricing_tier, and an `agent_endpoint` URL. Returns a descriptive \"no results\" string if nothing matches.\n\n## Transports\n\n- `POST /mcp` — Streamable HTTP (primary). Compatible with Claude Desktop, Cursor, and any spec-compliant MCP client. Add `{ \"url\": \"https://api.advocatemcp.com/mcp\", \"transport\": \"http\" }` to MCP client config.\n- `GET /mcp` with `Accept: text/event-stream` — SSE handshake (older transport). Handled by the same `StreamableHTTPServerTransport`.\n- `GET /mcp` without SSE header — returns a human-readable JSON info page listing the tools and connect instructions.\n\n## Discovery\n\n`/.well-known/ai-agent.json` is served by the Worker on every business domain. It includes `mcp_endpoint` pointing to Railway `/mcp`. This is the discovery surface for AI tools that check for agent endpoints before scraping.\n\n## Session 3 — directory-submission hardening\n\n**Shipped 2026-04-18.**\n\n- **Manifest endpoint** — `/.well-known/mcp.json` (Session 8). Directories that crawl capability manifests get tool schemas, transport, rate limits, auth modes, and the attribution endpoint from a single GET.\n- **Per-IP rate limit** — sliding 60-request-per-minute window keyed on `cf-connecting-ip`.\n  - **Primary:** `worker/src/lib/mcpRateLimitDO.ts` (`McpRateLimiterDO` Durable Object). A single global DO instance (id `mcp-rate-limiter-v1`) coordinates counters across every CF edge so the per-IP cap is globally coherent.\n  - **Fallback:** `worker/src/lib/mcpRateLimit.ts` (in-memory per CF isolate). Kicks in when the DO is unreachable (outage, rollout, missing binding). Imperfect — multi-edge traffic during a DO outage could multiply an attacker's effective limit — but it beats zero enforcement.\n  - Exceeded requests return `429 { error: \"rate_limited\", retry_after_seconds }` with `Retry-After`, `X-RateLimit-Limit`, and `X-RateLimit-Remaining` response headers.\n- **Structured logging** — every `/mcp` proxy emits one JSON line: `{ metric: \"mcp_proxy\", path, method, status, latency_ms, remaining, source }` where `source` is `\"do\"` (DO-backed) or `\"isolate_fallback\"` (DO miss). Rate-limited requests log as `mcp_rate_limited`; DO outages log as `mcp_rate_limit_do_unreachable`; proxy errors as `mcp_proxy_error`. Queryable via Cloudflare observability.\n- **Railway-side tool logging** — still provided by `agent_requests` (Session 11). `withAgentRequestLog` wraps every tool invocation with latency, outcome, and agent attribution.\n\n### Tuning the rate limit\n\nDefaults live in `worker/src/lib/mcpRateLimit.ts` (used by both the DO and the in-memory fallback):\n\n- `DEFAULT_LIMIT = 60` requests\n- `DEFAULT_WINDOW_MS = 60_000` — 60 seconds\n- `DEFAULT_MAX_IPS = 10_000` — LRU cap on tracked clients\n\nTo raise or lower, edit the constants and redeploy the Worker. A true runtime knob (env var or wrangler.toml `[vars]`) is a follow-up once a real abuse signal argues for it.\n\n### First-time DO deploy\n\nThe DO is registered via the `[[migrations]]` stanza in `wrangler.toml` (`tag = \"v1-mcp-rate-limiter\"`, `new_classes = [\"McpRateLimiterDO\"]`). The first `wrangler deploy` after this lands creates the DO class on Cloudflare; subsequent deploys reuse it. If additional DO classes are added later, append a new `[[migrations]]` stanza with a monotonically-increasing tag — do not mutate the existing one.\n\n### Submitting to directories\n\nWith the hardening above, the `/mcp` endpoint at `https://api.advocatemcp.com/mcp` is ready for submission to:\n\n- **Smithery** — `https://smithery.ai/` — submit via their web form\n- **PulseMCP** — `https://pulsemcp.com/` — submit via their web form\n- **Anthropic MCP registry** — `https://github.com/modelcontextprotocol/servers` — PR to the community-maintained list\n\nEach submission should reference the manifest at `/.well-known/mcp.json`, the tools listed above, and the rate-limit posture (60 req/min per IP).\n\n## Updating this doc\n\nUpdate this file at the end of any session that touches `server/src/routes/mcp.ts`, `worker/src/lib/mcpRateLimit.ts`, or the `/mcp` proxy in `worker/src/index.ts`.\n\n## Source: docs/response-generation.md\n\n# Response Generation\n\n## What exists today\n\nResponse generation lives entirely on Railway in `server/src/agent/`. Two files do all the work: `builder.ts` constructs the Claude system prompt; `query.ts` calls the Claude API, logs the exchange, and returns the result.\n\n## Intent classification\n\nEvery query is classified into one of six intents before the system prompt is built. Classification runs in `detectIntent()` in `server/src/agent/query.ts` using keyword matching evaluated in priority order:\n\n1. **brand_direct** — query contains the business's name\n2. **emergency** — contains: emergency, urgent, asap, 24/7, right now, tonight, immediately\n3. **affordable** — contains: cheap, affordable, budget, low cost, how much, price, cost, inexpensive\n4. **best_top** — contains: best, top, recommended, highest rated, top-rated, top rated\n5. **specific_service** — query mentions a service from `top_services` or `services` fields\n6. **general** — default fallback\n\nThe intent is stored in `queries.intent` on every insert. **Do not build a parallel classifier.** The canonical implementation is `detectIntent()` in `server/src/agent/query.ts`.\n\n## System prompt construction — three layers\n\n`buildSystemPrompt(business, intent, crawlerAgent?)` in `server/src/agent/builder.ts` assembles a prompt in three layers. Each layer is additive; none replaces the one above it.\n\n**Layer 1 — Profile block.** Built dynamically from non-null fields on the `BusinessRow`. Fields included: name, description, services, category, location, star_rating + review_count, years_in_business, top_services, availability, differentiator, certifications, pricing_tier, pricing, service_radius_miles, service_area_keywords, phone, referral_url/website. Also surfaces 9-step wizard JSON blobs (see bottom section).\n\n**Layer 2 — Intent emphasis.** Intent-specific instruction injected above the response structure via `getIntentEmphasis()`. Examples: `best_top` leads with the star rating; `emergency` leads with availability and response time; `affordable` leads with pricing tier; `brand_direct` gives a full profile overview and suppresses invented reputation language if no rating data exists; `specific_service` focuses on the matched service first.\n\n**Layer 3 — Per-bot emphasis.** Crawler-specific formatting guidance dispatched by `getBotPromptBlock()` in `server/src/prompts/index.ts`. One module per bot class:\n\n| Module | Covers | Shape |\n|---|---|---|\n| `perplexity.ts` | PerplexityBot | Citation-heavy bullets, ≤180 words |\n| `openai.ts` | GPTBot, OAI-SearchBot | Conversational paragraphs, inline facts |\n| `claude.ts` | ClaudeBot | Clean markdown, one H2 + bullets |\n| `google.ts` | Googlebot, Google-Extended | SERP-snippet-first lead sentence |\n| `training.ts` | anthropic-ai, cohere-ai, meta-externalagent | Factual baseline for training ingestion |\n| `default.ts` | anything else / null / empty | Empty emphasis (current pre-Session-2 behaviour preserved) |\n\nThe per-bot block appears at the very end of the system prompt, after the fixed rules, labeled `CRAWLER-SPECIFIC FORMATTING:`. Unknown or missing bot input falls through to `default` → no emphasis appended → behaviour byte-identical to pre-Session-2.\n\nThe response structure instruction asks Claude for a 5-part flow: direct answer → social proof → services + differentiator → trust signals → call to action with the referral link. Max 150 words, optimized for AI citation.\n\n## Bot identity normalization\n\nThe Cloudflare Worker normalizes bot identity at the edge. `worker/src/index.ts` calls `crawlerName(ua)` to map the raw User-Agent substring (e.g. `\"Mozilla/5.0 ... PerplexityBot/1.0\"`) to the canonical name from the `AI_CRAWLERS` array (`\"PerplexityBot\"`), then forwards only the canonical name to Railway in the POST body's `crawler` field. Railway's `POST /agents/:slug/query` zod schema accepts `crawler?: string` up to 200 chars. Non-bot traffic or unmatched UAs get an empty string, which `getBotPromptBlock` treats as \"no per-bot block.\"\n\n## Claude API call\n\n`queryAgent(business, query, crawlerAgent)` in `server/src/agent/query.ts`:\n- Model: `claude-sonnet-4-6` (overridable via `MODEL` env var)\n- Max tokens: 512\n- Single-turn: system prompt + user message only (no conversation history)\n- **Prompt caching enabled** via `system: [{ type: \"text\", text, cache_control: { type: \"ephemeral\" } }]` — since `{ bot, intent, business }` is a deterministic tuple, repeat queries for the same tuple hit cache\n- Response is logged synchronously to `queries` table via `better-sqlite3`\n- Returns: `{ response, referral_url, business, business_slug, intent, timestamp, powered_by, query_id }`\n\n## Updating this doc\n\nUpdate this file at the end of any session that touches `builder.ts`, `query.ts`, or adds per-bot prompt files.\n\n## 9-step wizard fields (April 2026)\n\nThe onboarding wizard now persists these JSON blobs on the business row:\n- `hours_json` — week schedule + `emergency_24_7` flag\n- `services_json_v2` — inclusions / exclusions / specialties / not_offered\n- `pricing_json_v2` — ranges, `free_estimates`, `call_for_quote`\n- `credentials_json` — licenses, insured, bonded, certifications\n- `ratings_json` — Google + Yelp separately\n- `customer_quotes_json`, `case_stories_json`, `lead_routing_json`\n\n`buildSystemPrompt` surfaces them via `parseJsonSafe`; malformed JSON is silently ignored. Intent branching: emergency → `hours_json.emergency_24_7`; affordable → `pricing_json_v2.ranges`; best_top → `ratings_json` + `credentials_json.licenses`.\n\n## Source: docs/manual-onboarding-runbook.md\n\n# Manual Onboarding Runbook\n\nStep-by-step for admin-provisioned customers — the \"white-glove\" path where\nyou set up a tenant directly instead of sending them through the self-serve\nwizard at `advocatemcp.com/audit` / `advocatemcp.com/onboarding`.\n\nThis is the path that was used for **Example Agency** (WCC) as the first\nreal paying customer.\n\nLast validated: 2026-04-19.\n\n---\n\n## When to use this\n\n- First real customer in a new segment (you want control over the profile\n  content, category, differentiators).\n- Custom-domain customer (their own `www.foo.com`, not\n  `foo.hosted.advocatemcp.com`).\n- Prospect you closed via outbound who isn't going to self-serve through\n  the wizard.\n\nFor self-serve signups, the wizard at `/audit` → `/onboarding` handles\neverything automatically; this runbook doesn't apply.\n\n---\n\n## Prerequisites\n\n- `$ADMIN_SECRET` — from 1Password (matches the worker's `ADMIN_SECRET`).\n- `$API_KEY` — the shared worker↔server admin key (for Railway `/register`).\n- Laptop access to Cloudflare dashboard (for DNS + Workers Routes).\n- Access to Railway dashboard if you need to eyeball SQLite directly.\n\n---\n\n## The flow, end to end\n\n```\n1. Register the agent on Railway        (creates businesses row + api_key)\n2. Create the D1 business row            (so the worker can route traffic)\n3. Create the user + grant access        (so they can log into the dashboard)\n4. Set up the custom hostname on CF      (if custom-domain tenant)\n5. Ensure the Worker Route               (so CF routes their traffic to us)\n6. Verify end-to-end                     (crawler interception + dashboard)\n```\n\nEach step is idempotent where possible — re-running is safe.\n\n---\n\n## Step 1: Register the agent on Railway\n\nThis creates the canonical business record on the Railway side. Railway\nreturns a `slug` and `api_key` we use everywhere else.\n\n```bash\ncurl -X POST https://api.advocatemcp.com/register \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Authorization: Bearer $API_KEY\" \\\n  -d '{\n    \"name\": \"Example Agency\",\n    \"description\": \"Boise copywriting studio specializing in DTC email sequences.\",\n    \"category\": \"copywriter\",\n    \"location\": \"Boise, ID\",\n    \"services\": [\"email copy\", \"landing page copy\", \"brand voice\"],\n    \"phone\": \"208-555-0100\",\n    \"website\": \"https://www.example-tenant.com\",\n    \"star_rating\": 4.9,\n    \"review_count\": 18,\n    \"tone\": \"warm\",\n    \"plan\": \"pro\",\n    \"email\": \"owner@example-tenant.com\"\n  }'\n```\n\n**Save the response** — you'll paste `slug` and `api_key` in step 2 and 3.\n\n```json\n{\n  \"slug\": \"example-tenant\",\n  \"api_key\": \"2a3b4c...\",\n  \"agent_endpoint\": \"https://api.advocatemcp.com/agents/example-tenant/query\",\n  ...\n}\n```\n\n### Optional — richer profile\n\nFor agency + law-firm tenants, pass the 9-step wizard blobs\n(`hours_json`, `credentials_json`, `ratings_json`, `pricing_json_v2`, etc.)\nin the same body. See `docs/followups.md` Task 10 for a full sample payload.\n\n### On collision\n\nIf another tenant already has the generated base slug, Railway auto-suffixes\n(`-1`, `-2`, …) — check the returned slug before pasting it into step 2.\n\n---\n\n## Step 2: Create the D1 business row\n\nRailway has the profile + `api_key`; now the worker needs a pointer so it\nknows to route `www.example-tenant.com` traffic to that slug.\n\n`/admin/create-client` creates the user AND the D1 business row AND grants\nthe user access, in one call:\n\n```bash\ncurl -X POST https://customers.advocatemcp.com/admin/create-client \\\n  -H \"Authorization: Bearer $ADMIN_SECRET\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"email\":         \"owner@example-tenant.com\",\n    \"password\":      \"<temporary-strong-password>\",\n    \"full_name\":     \"Jane Owner\",\n    \"slug\":          \"example-tenant\",\n    \"business_name\": \"Example Agency\",\n    \"api_key\":       \"<the api_key Railway returned>\",\n    \"role\":          \"client\"\n  }'\n```\n\nThe endpoint is idempotent: if the user already exists, the password is\nupdated; if the business row already exists, just the access grant is\nensured.\n\n**Send the temporary password to the customer via a secure channel** (1Password\nSend link, password-manager share, or have them reset immediately via the\nforgot-password flow on `/login`).\n\n---\n\n## Step 3: Set up the custom hostname (custom-domain tenants only)\n\nSkip this section for hosted tenants — `foo.hosted.advocatemcp.com` is\nauto-provisioned and you don't set it up manually.\n\n### 3a. Register the custom hostname with Cloudflare\n\n```bash\ncurl -X POST https://customers.advocatemcp.com/api/onboard \\\n  -H \"X-Admin-Secret: $ADMIN_SECRET\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"domain\": \"www.example-tenant.com\",\n    \"slug\":   \"example-tenant\",\n    \"name\":   \"Example Agency\",\n    \"email\":  \"owner@example-tenant.com\"\n  }'\n```\n\nResponse includes `cloudflare.txtName`, `cloudflare.txtValue`, and\ninstructions for the customer.\n\n### 3b. Tell the customer to add the DNS records\n\n- **TXT record for ownership verification** — `txtName` / `txtValue` from the\n  response.\n- **CNAME record** from `www.example-tenant.com` → `customers.advocatemcp.com`.\n\nSend those two lines via email.\n\n### 3c. Wait for verification\n\nCF checks the TXT + CNAME every few minutes. Monitor status:\n\n```bash\ncurl https://customers.advocatemcp.com/api/onboard/www.example-tenant.com/status \\\n  -H \"X-Admin-Secret: $ADMIN_SECRET\"\n```\n\nStatus transitions: `pending_verification` → `active` once CF sees the\nrecords and issues the SSL cert (2–10 min typical).\n\n---\n\n## Step 4: Ensure the Worker Route\n\nCF SaaS doesn't automatically wire new custom hostnames to our Worker. The\nhostname needs a Workers Route pattern `{hostname}/*` on the\n`advocatemcp.com` zone. This is the step that was missing pre-Apr 16 2026\n(see `docs/followups.md`).\n\n### 4a. Confirm `CF_API_TOKEN` has `Workers Routes: Edit` scope\n\nDashboard → My Profile → API Tokens → edit the token → add\n`Account > Workers Routes > Edit` on the `advocatemcp.com` zone if it\nisn't there. Save.\n\n### 4b. Create the route\n\n```bash\ncurl -X POST https://customers.advocatemcp.com/admin/domains/ensure-worker-route \\\n  -H \"X-Admin-Secret: $ADMIN_SECRET\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"hostname\":\"www.example-tenant.com\"}'\n```\n\nExpected `200 OK` with `{ \"created\": true, \"pattern\": \"www.example-tenant.com/*\", ... }`.\n\nIf this returns 502 or \"not configured\", the `CF_API_TOKEN` is still\nunder-scoped. Re-check step 4a.\n\n---\n\n## Step 5: Verify end-to-end\n\n### 5a. Crawler interception (custom-domain only)\n\n```bash\ncurl -I -A \"PerplexityBot/1.0\" https://www.example-tenant.com/\n```\n\nExpected: `200` from our Worker (not a 522 CF error, not the customer's\norigin HTML). The Worker should serve a redirect or response shape\nspecific to AI crawler UAs.\n\n### 5b. Public profile endpoint\n\n```bash\ncurl https://api.advocatemcp.com/agents/example-tenant/profile | jq .\n```\n\nExpected: structured JSON with `name`, `description`, `services`, `category`.\n\n### 5c. Agent query\n\n```bash\ncurl -X POST https://api.advocatemcp.com/agents/example-tenant/query \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Authorization: Bearer $THE_TENANT_API_KEY\" \\\n  -d '{\"query\": \"Who are the best copywriters for DTC email in Boise?\",\n       \"crawler_agent\": \"PerplexityBot\"}'\n```\n\nExpected: 200 with `response` mentioning the business, referral link, and\ncategory-appropriate emphasis.\n\n### 5d. Dashboard\n\nLog in at `https://customers.advocatemcp.com/login` with the email +\ntemporary password from step 2. The Overview section should render without\nerrors and show the tenant's slug in the breadcrumb.\n\n---\n\n## Rollback\n\nIf anything in steps 1–4 wrote partial state and you need to start over:\n\n1. **Delete the Railway row**: `DELETE FROM businesses WHERE slug='<slug>';`\n   (via Railway's SQLite shell — get there from the dashboard → Data).\n2. **Delete the D1 row**: `npx wrangler d1 execute advocatemcp-auth --remote\n   --command=\"DELETE FROM businesses WHERE slug='<slug>'\"` from `worker/`.\n3. **Delete the user access**: `DELETE FROM user_business_access WHERE user_id='<id>'`\n   same way. Keep the user row unless they asked to be removed entirely.\n4. **Delete the CF custom hostname**: `DELETE /admin/onboard/<domain>/disable`\n   via the admin API (or CF dashboard manually).\n5. **Delete the Worker Route**: currently done by hand in the CF dashboard\n   (Workers & Pages → routes for the zone).\n\nThen start at step 1 again.\n\n---\n\n## Troubleshooting\n\n### \"Admin API key not configured\"\n\n`ADMIN_SECRET` isn't set on the worker. `cd worker && npx wrangler secret put\nADMIN_SECRET` from a freshly-generated `openssl rand -hex 32`.\n\n### Railway `/register` returns 400 \"validation_error\"\n\nThe payload is missing a required field. Check `server/src/schemas/business.ts`\nfor the full zod schema. Most common misses: `star_rating`, `review_count`,\n`category`.\n\n### Railway returns 500 and Stripe webhook retried before you finished\n\nThe webhook registered a tenant with empty `profile` fields; the agent\nworks but gives thin responses. Use\n`POST /admin/onboard/retry-railway` (see PR #75) once you've populated the\nKV tenant record via `POST /api/onboard`.\n\n### The customer's domain returns 522 after DNS propagates\n\nWorker Route missing — re-run step 4b. If that 502s, token scope (4a).\n\n### Dashboard shows \"No business found for this account\"\n\nThe user→business access grant didn't commit. Re-run step 2 — the endpoint\nis idempotent for existing users and the `grantAccess` call runs\nunconditionally.\n\n---\n\n## Related docs\n\n- `docs/secrets-runbook.md` — secret rotation + inventory.\n- `docs/dns-routing.md` — deeper architecture on DNS / Workers Routes.\n- `docs/audit-funnel.md` — the self-serve onboarding path (alternative to\n  this).\n- `docs/followups.md` — known issues + pending operator steps.\n\n# DNS setup guides (per provider)\n\n\n## Source: site/js/dns-provider-guides.js\n\n,\n *     login_url:\n\n,\n *     apex_strategy:\n\n,\n      login_url:\n\n,\n      apex_strategy:\n\n,\n      auto_dns: true,\n      www_steps: [\n        { type:\n\nSign in at godaddy.com.\n\nClick the avatar in the top right → 'My Products'.\n\nFind your domain in the list, click 'DNS' next to it.\n\nScroll to the Records section, click 'Add'.\n\nType: CNAME. Name: www. Value: {{cname_target}}. TTL: 1 hour. Save.\n\nGoDaddy doesn't natively support ANAME / ALIAS, so we use Domain Forwarding to redirect apex traffic to www. Bots follow the redirect to the optimized response — same outcome as a true ANAME record.\n\nFrom your domain's overview page, click the 'Forwarding' section.\n\nClick 'Add Forwarding'.\n\n},\n        { type:\n\nDo NOT pick 'Update my nameservers' — that breaks the rest of your DNS records you just added.\n\nBack in the DNS Management page, click 'Add' under Records again.\n\nType: TXT. Name: {{txt_host_name}}. Value: {{txt_value}}. TTL: 1 hour. Save.\n\nGoDaddy will append .yourdomain.com automatically — paste only the part before the dot.\n\nIf you ever moved your DNS off GoDaddy, the records won't apply. Check 'Nameservers' on your domain overview — they should be ns##.domaincontrol.com.\n\nGoDaddy caches TTLs aggressively. Allow up to 1 hour for propagation even if you set TTL to 600.\n\n,\n      auto_dns: false,\n      www_steps: [\n        { type:\n\nSign in at squarespace.com.\n\nOpen Settings → Domains. Click your domain.\n\nClick 'DNS Settings' (or 'Custom DNS' on older accounts).\n\nClick 'Add Record'. Type: CNAME. Host: www. Data: {{cname_target}}. Save.\n\nSquarespace's built-in DNS doesn't support ANAME / ALIAS. The cleanest path is to move DNS to Cloudflare (free, 10 min) — that gives you proper apex CNAME flattening AND a free CDN on top.\n\nSign up at cloudflare.com (free plan).\n\nClick 'Add a site', enter {{apex}}, pick the Free plan.\n\nCloudflare scans your existing DNS records — review them, click 'Continue'. They migrate over so your site stays working.\n\nCloudflare gives you 2 nameservers (e.g., lola.ns.cloudflare.com).\n\nBack in Squarespace → Domains → 'Advanced settings' → 'Use Custom Nameservers'. Paste both Cloudflare nameservers. Save.\n\nWait ~10 min - 24h for the nameserver change to propagate. Re-run AdvocateMCP setup — we'll detect you're on Cloudflare and add records automatically (no manual apex setup needed).\n\nSwitching to Cloudflare nameservers does NOT affect your Squarespace site — your site keeps working. It only changes who answers DNS queries.\n\nIn Squarespace's DNS panel: Add Record. Type: TXT. Host: {{txt_host_name}}. Data: {{txt_value}}. Save.\n\nIf you moved DNS to Cloudflare per the apex steps above, add the TXT record in Cloudflare's DNS panel instead.\n\nSquarespace's DNS panel sometimes hides the apex record (@) from new accounts. If you don't see it, contact Squarespace support OR switch to Cloudflare nameservers — easier than fighting their UI.\n\nSign in at namecheap.com.\n\nDomain List → 'Manage' next to your domain → 'Advanced DNS' tab.\n\nClick 'Add New Record'. Type: CNAME Record. Host: www. Value: {{cname_target}}. TTL: Automatic. Save.\n\nNamecheap supports an 'ALIAS Record' on their FreeDNS / BasicDNS that works at the apex — that's the cleanest path.\n\nIn Advanced DNS → 'Add New Record'.\n\nType: ALIAS Record. Host: @. Value: {{cname_target}}. TTL: Automatic. Save.\n\nType: TXT Record. Host: {{txt_host_name}}. Value: {{txt_value}}. TTL: Automatic. Save.\n\nNamecheap requires Email Forwarding to be set up before you can add MX records. Doesn't affect us, but if you see surprising MX warnings, that's why.\n\nSign in at dash.cloudflare.com.\n\nPick your domain → DNS → Records.\n\nClick 'Add record'. Type: CNAME. Name: www. Target: {{cname_target}}. Proxy status: DNS only (gray cloud). Save.\n\nCloudflare's CNAME flattening works at the apex — you can CNAME the root of your zone directly to {{cname_target}}, no ANAME needed.\n\nClick 'Add record'. Type: CNAME. Name: @ (or your bare domain). Target: {{cname_target}}. Proxy status: DNS only (gray cloud). Save.\n\nKeep proxy status set to 'DNS only' (gray cloud), not 'Proxied' (orange cloud) — Cloudflare-proxied traffic conflicts with our SaaS routing on the same edge.\n\nClick 'Add record'. Type: TXT. Name: {{txt_host_name}}. Content: {{txt_value}}. TTL: Auto. Save.\n\nIf you use Cloudflare for everything (DNS + CDN + email), be careful not to enable proxy on the records pointing at AdvocateMCP — that double-proxies and produces weird headers.\n\nSign in at wix.com.\n\nDomains → click your domain → Advanced → DNS Records.\n\nAdd a CNAME record. Host name: www. Points to: {{cname_target}}. Save.\n\nWix has limited apex support and doesn't expose ANAME / ALIAS. The cleanest path is to switch DNS to Cloudflare (free) so we can use CNAME flattening at the apex — same approach as Squarespace.\n\nSign up at cloudflare.com (free plan), add {{apex}} as a site.\n\nCloudflare scans your existing DNS, pre-populates records, gives you 2 nameservers.\n\nBack in Wix → Domains → your domain → Advanced → 'Change Name Servers'. Paste the Cloudflare nameservers. Save.\n\nWait 10 min - 24h for propagation. Re-run AdvocateMCP setup — we'll detect Cloudflare and add records automatically with no manual apex work.\n\nIn Wix DNS Records (or Cloudflare's DNS panel if you switched): add a TXT record. Host name: {{txt_host_name}}. Value: {{txt_value}}. Save.\n\nWix requires you to be on a paid Wix plan to manage DNS records on a domain registered through them.\n\nGoogle Domains\n\nGoogle Domains was sold to Squarespace in 2023. If you're still on Google's interface, your domain is being migrated — you may want to follow the Squarespace guide instead.\n\nSign in at domains.google.com (if still active for your account).\n\nClick your domain → DNS → 'Manage custom records'.\n\nAdd: Host: www. Type: CNAME. TTL: 1H. Data: {{cname_target}}. Save.\n\nIn 'Manage custom records', add a synthetic record at @ pointing to {{cname_target}}.\n\nGoogle Domains' Cloud DNS supported ANAME-equivalent A-record synthesis. After migration to Squarespace, this stops working — switch to the Squarespace guide once you see the new UI.\n\nBetter long-term: move DNS to Cloudflare. Sign up at cloudflare.com (free), add {{apex}}, copy the 2 nameservers, swap them in at Google Domains. Re-run AdvocateMCP setup once propagated — we'll auto-add records.\n\nAdd a custom record. Host: {{txt_host_name}}. Type: TXT. TTL: 1H. Data: {{txt_value}}. Save.\n\nIf your Google Domains UI redirects you to a Squarespace login, your domain has been migrated. Switch to the Squarespace guide.\n\nAWS Route 53\n\nSign in to AWS Console → Route 53 → Hosted zones.\n\nClick your hosted zone → 'Create record'.\n\nRecord name: www. Record type: CNAME. Value: {{cname_target}}. TTL: 300. Save.\n\nRoute 53 has native ALIAS records that work at the apex. Use ALIAS, not A or CNAME, at the root.\n\nClick 'Create record'. Record name: leave empty (apex). Record type: A. 'Alias' toggle: ON. Route traffic to: 'Alias to another record'. Region: pick the closest. Type {{cname_target}} into the alias target. Save.\n\nIf the ALIAS dropdown doesn't accept {{cname_target}} directly (Route 53 typically wants ALB/CloudFront/S3 endpoints), fall back to a static A record using the IPs we list under 'Other DNS providers' — or contact us for the current values.\n\nClick 'Create record'. Record name: {{txt_host_name}}. Record type: TXT. Value: {{txt_value}} (wrap in double-quotes). TTL: 300. Save.\n\nRoute 53 charges per hosted zone (~$0.50/month). If the cost annoys you, switching to Cloudflare DNS (free) is straightforward — point your domain registrar at Cloudflare's nameservers.\n\nShopify's DNS panel doesn't expose enough record types for AdvocateMCP setup. The cleanest fix is to move your domain's DNS to Cloudflare (free), keep Shopify as your store host, and we'll add records via Cloudflare automatically afterward.\n\nCloudflare scans your existing DNS and pre-populates the records — review them, click 'Continue'.\n\nIn your domain registrar (NOT Shopify — wherever you actually bought the domain), change the nameservers to Cloudflare's two values.\n\nWait ~10 min - 24h for nameservers to propagate. Then re-run AdvocateMCP setup. We'll detect Cloudflare and add records automatically.\n\nAfter Cloudflare migration above, our auto-DNS handles apex via CNAME flattening — no extra step on your end.\n\nAfter Cloudflare migration, our auto-DNS adds the SSL TXT record automatically.\n\nSwitching nameservers does NOT take down your Shopify store — it stays on Shopify, you just change who answers DNS queries about your domain.\n\nHostGator manages DNS via cPanel and doesn't expose a public API. Cleanest path: move DNS to Cloudflare (free), keep HostGator as your hosting.\n\nSign up at cloudflare.com (free), add {{apex}}, copy the 2 Cloudflare nameservers.\n\nSign in at portal.hostgator.com → My Domains → click your domain → Nameservers → 'Change Nameservers'.\n\nReplace HostGator's nameservers with the Cloudflare ones. Save.\n\nWait ~10 min - 24h for propagation. Re-run AdvocateMCP setup; we'll detect Cloudflare and finish automatically.\n\nCloudflare flattening at apex handles routing once you complete the migration above.\n\nWe add the SSL TXT record via the Cloudflare auto-DNS flow after migration.\n\nHostGator's DNS UI sometimes lives under 'cPanel → Zone Editor' and sometimes under 'My Domains → DNS' depending on plan tier. The Cloudflare migration sidesteps that variance.\n\nBluehost manages DNS via their cPanel-based panel and doesn't expose a public API. Move DNS to Cloudflare (free) for the cleanest setup.\n\nSign in at my.bluehost.com → Domains → click your domain → Name Servers.\n\nPick 'Use Custom Nameservers'. Paste the Cloudflare ones. Save.\n\nWait ~10 min - 24h. Re-run AdvocateMCP setup; we'll detect Cloudflare and finish.\n\nCloudflare flattening handles apex once you've completed the migration above.\n\nAuto-handled by our Cloudflare auto-DNS flow after migration.\n\nLike other Newfold-owned hosts (HostGator, Domain.com, Network Solutions), Bluehost's DNS UI is dated and inconsistent. Cloudflare's modern UI handles everything once you switch.\n\nSign in at my.ionos.com.\n\nDomains & SSL → click your domain → DNS.\n\nClick 'Add record'. Type: CNAME. Host name: www. Points to: {{cname_target}}. TTL: 3600. Save.\n\nIONOS DNS doesn't support ANAME at apex, so we route apex via static A records pointing at our anycast IPs.\n\nClick 'Add record'. Type: A. Host name: @ (or leave empty). Value: 104.21.44.57. Save. Repeat for 172.67.195.220.\n\nStatic A records work today but Cloudflare anycast IPs can rotate. Re-check this guide every few months.\n\nClick 'Add record'. Type: TXT. Host name: {{txt_host_name}}. Content: {{txt_value}}. TTL: 3600. Save.\n\nIONOS sometimes shows DNS records under 'Settings → DNS' rather than at the top level. If you don't see DNS listed, dig deeper into the domain's settings panel.\n\nyour DNS provider\n\nSign in to your DNS provider's control panel (the place where you bought your domain, or wherever you've moved DNS to since).\n\nFind the section labeled DNS, DNS Zone, or Records — every provider names it slightly differently.\n\nAdd a CNAME record. Name: www. Value (or 'Points to'): {{cname_target}}. TTL: leave default or set to 1 hour. Save.\n\nDNS doesn't allow CNAME at the zone apex (the root of your domain, with no www). So apex needs special handling. In rough order of preference:\n\nOption 1 — ANAME / ALIAS / CNAME-flattening: if your DNS provider supports any of these record types, add one at @ (apex) pointing to {{cname_target}}. Cleanest.\n\nOption 3 — Cloudflare nameservers: sign up at cloudflare.com (free), add your domain, switch your registrar's nameservers to Cloudflare's. Inside Cloudflare, you get apex CNAME flattening for free.\n\nOption 4 — Static A records: in your DNS panel, add two A records at @ (apex) pointing to 104.21.44.57 and 172.67.195.220. These are the same anycast IPs that {{cname_target}} resolves to, so apex traffic lands on our edge the same way www does.\n\nStatic A records work today but Cloudflare anycast IPs can rotate. Re-check this guide every few months, or use options 1-3 for long-term stability.\n\nAdd a TXT record. Host / Name: {{txt_host_name}}. Value: {{txt_value}}. TTL: default. Save.\n\nSome providers expect the host with a trailing dot (e.g. _cf-custom-hostname.acme.com.) — if your provider rejects the value, try with and without the trailing dot.\n\nTell us your provider so we can add a guide. Email max@advocate-mcp.com — usually only takes us a day to add new providers.";

export const KNOWLEDGE_BASE_META = {
  generated_at:    "2026-04-27T04:54:23.783Z",
  bytes:           104077,
  approx_tokens:   26019,
  source_count:    15,
};
